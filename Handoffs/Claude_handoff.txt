═══════════════════════════════════════════════════════
RSB PROJECT IDENTITY
═══════════════════════════════════════════════════════

Project:    RSB — disciplined sportsbook analytics / +EV simulation
Repo:       https://github.com/Rryan0613/RSB
Local path: /Users/ryan/Desktop/PersonalWebsite/RSB/worldcup_ai_v0_1
Python:     3.13 virtual environment (.venv)
Shell:      use .venv/bin/python for Python/test commands

RSB is:

* a disciplined sportsbook analytics / +EV simulation project
* a research-backed model, simulation, backtest, and review system
* eventually intended to help build data-backed picks and parlays with reasoning behind every leg

RSB is not:

* a lock generator
* a random pick generator
* a parlay generator yet
* a frontend/API product yet
* a live betting bot
* an auto-betting system

Long-term vision:
The final version of RSB should help build research-backed, data-backed picks and parlays.

The goal is to move away from casual assumptions like:
"Mbappé is a star, so he'll score."

Instead, RSB should eventually justify every candidate leg using structured evidence such as:

* model probability
* sportsbook implied probability
* estimated edge
* historical form
* xG / shot / chance quality
* opponent profile
* lineup / injury / rotation context
* tactical / formation context
* market semantics
* risk flags
* data quality flags
* correlation risk if used in a parlay

Parlays are part of the eventual long-term scope, but not yet.

Future parlay logic must be:

* data-backed
* correlation-aware
* edge-aware
* risk-adjusted
* capable of rejecting bad slips instead of forcing action

RSB should never become reckless or vibe-based. It should prefer pass/no bet when the edge is weak.

Long-term automation goal:
The user should only need to provide high-level run parameters such as sport, date range,
markets, sportsbooks, and risk preferences. RSB should eventually collect fixtures, pull
sportsbook odds, pull stats/injuries/lineups, build features, run models, calculate implied
probabilities and edge, evaluate candidates, and produce ranked outputs automatically.
Current primitive functions take caller-supplied values only because they are being developed
and tested in isolation. Later internal pipeline modules will supply those values automatically.
The user will never manually input edge values, implied probabilities, or player stats
in the final system.

Long-term self-learning goal:
RSB should eventually learn from its own prediction history through controlled feedback loops.
The intended self-learning cycle is:

1. Make prediction / candidate evaluation (pre-game snapshot)
   - record odds, model probability, implied probability, edge, features, context
2. Ingest final result
   - hit / miss, final score / stat outcome, closing line, market movement
3. Run structured post-result review
   - classify why a leg missed: variance, bad calibration, injury surprise,
     line movement, wrong market semantics, weak data, feature gap, correlation issue
4. Store structured review notes and miss reasons in the database
5. Accumulate history and use it to improve:
   - model calibration
   - feature engineering
   - pass/reject rules
   - market selection rules
   - data quality rules
   - parlay correlation rules

RSB should NOT learn recklessly:
* Bad: "One leg missed — change the model immediately."
* Good: "Track this miss, classify why it happened, accumulate evidence across many
         outcomes, then recalibrate only when patterns are statistically meaningful."

Sports are noisy. A good bet can lose. A bad bet can win. RSB must learn from patterns
over many outcomes, not overreact to individual misses.

The primitives already built — review_taxonomy.py, review_notes.py, odds.py, edge.py,
candidate_evaluation.py, backtest_review.py, ev.py, prop_candidate.py, odds_snapshot.py,
prop_result.py, candidate_ev.py, candidate_ranking.py, candidate_report.py, and
market_capability.py — are early vocabulary for the future self-learning loop.

═══════════════════════════════════════════════════════
RSB NORTH STAR / PRODUCT MINDSET
═══════════════════════════════════════════════════════

RSB's long-term goal is to become an autonomous, research-backed sportsbook analytics system that can identify and rank the best +EV prop/pick candidates.

The desired final product should eventually be able to:

* ingest sportsbook odds and player prop markets
* ingest or connect to player/team stats, injuries, lineups, availability, matchup context, and historical results
* run simulations/models/backtests/calibration checks
* compare model probability against sportsbook implied probability
* calculate EV/edge
* rank candidate props/picks by expected value, confidence, data quality, and risk
* construct analytically coherent parlays from compatible candidates
* reject bad parlays even when individual legs look decent
* explain why each candidate or parlay is worth considering or why it should be passed
* track whether the model has genuine edge through CLV, calibration, and settled results
* prefer no-bet/pass when data quality, calibration, or edge is insufficient

RSB is not intended to stay as disconnected primitives forever. The primitive-by-primitive process is intentional because the final predictor must be disciplined and auditable, not a fake lock generator.

Important distinction:
When a version says it did not add live odds APIs, player prop data, sportsbook integrations, simulations, injuries, lineups, historical data, calibration, recommendations, or ranked picks, that means those items were out of scope for that specific version only. It does not mean those capabilities are banned from the project.

Current development philosophy:

* Build stable primitives first.
* Add schemas/contracts before wiring.
* Add provider ingestion only when internal records can validate it.
* Add ranking/recommendation only after odds, EV, data quality, calibration, and backtest review foundations are strong enough.
* Never call anything a lock.
* Do not force picks.
* The end goal is useful ranked +EV prop/pick candidates, but every step must preserve correctness, testability, and auditability.

═══════════════════════════════════════════════════════
ROADMAP PLANNING CADENCE — WORKFLOW DECISION
═══════════════════════════════════════════════════════

Going forward, RSB should plan 2-4 versions ahead at a time instead of one version at a time.

* Implementation still remains one version per branch and one PR at a time.
* Claude may propose a 2-4 version roadmap plan only when ChatGPT asks for it.
* ChatGPT remains the roadmap/scope gatekeeper.
* The approved roadmap batch (v0.2.11, v0.2.12, v0.3.0) is recorded below. All three are
  now complete. v0.3.0 shipped as the Candidate Evaluation Contract (PR #43, merge commit
  20f34ab), not the "Provider Source Contract / Identity Normalization Primitives" scope
  earlier handoffs predicted.
* UPDATE 2026-08-11: the previous directional sequence (v0.3.1 Legacy Candidate
  Translation, v0.3.2 Canonical Odds Snapshot Reconciliation, v0.3.3 Candidate Persistence
  Framework, v0.3.4 First MLB Runtime Skeleton) is superseded. v0.3.1 is now MLB Statcast
  Data Foundation. v0.3.2-v0.3.4 (MLB Plate-Appearance Dataset & Rate Foundation, MLB
  Plate-Appearance Probability Baseline, MLB Walk-Forward Evaluation & Calibration) remain
  a directional planning batch, not independently approved. See docs/MLB_NBA_ROADMAP.md.
* UPDATE 2026-08-12: v0.3.1 — MLB Statcast Data Foundation merged to main (PR #46, merge
  commit 3ae353f, implementation commit 765b5ed, 2156 tests passed). Feature branch
  feature/v0.3.1-mlb-statcast-data-foundation deleted locally and remotely. v0.3.2 — MLB
  Plate-Appearance Dataset & Rate Foundation is now the next roadmap objective, still
  directional and not implementation-approved. Next required action: perform the v0.3.2
  inspection/planning stage; ChatGPT must approve the detailed implementation plan before
  coding begins.
* UPDATE 2026-08-12 (later same day): v0.3.2 — MLB Plate-Appearance Dataset & Rate
  Foundation is COMPLETE. PR #48, merge commit b876eb1, implementation commit 9212c93,
  real-Savant correction commit ba3ce68, 2307 tests passed. Feature branch
  feature/v0.3.2-mlb-plate-appearance-dataset-rate-foundation deleted locally and
  remotely. v0.3.3 — MLB Plate-Appearance Probability Baseline is now the next roadmap
  objective. Implementation architecture is not approved — a separate v0.3.3
  inspection/planning stage is required before coding begins.
* UPDATE 2026-08-18: v0.3.3 — MLB Plate-Appearance Probability Baseline is COMPLETE.
  PR #50, merge commit dcab8bb, implementation commit 72c4a8f, 2380 tests passed (73 new
  probability tests). Feature branch feature/v0.3.3-mlb-pa-probability-baseline deleted
  locally and remotely. v0.3.4 — MLB Walk-Forward Evaluation & Calibration is now the
  next roadmap objective. Implementation architecture is not approved — a separate
  v0.3.4 inspection/planning stage is required before coding begins.

FUTURE MLB OPERATIONAL REQUIREMENTS (directional, NOT v0.3.4 scope) — full detail lives
in docs/MLB_NBA_ROADMAP.md §16, "Future MLB operational requirements". Preserved here as
a pointer so these are not lost across sessions:
  A. On-run historical synchronization/completeness at run time (last-complete-coverage
     detection, gap fill through current run date, configurable overlap/backfill window,
     idempotent reconciliation, fail-closed on incomplete/conflicted mandatory data). No
     background updater required.
  B. External production persistence: hosted PostgreSQL for structured data, object
     storage for large immutable raw/normalized snapshots; local files are temporary
     cache only; laptop is not the canonical warehouse; exact vendor undecided.
  C. Multi-source verification/reconciliation with provenance states (VERIFIED /
     UNVERIFIED / CONFLICTED / STALE), field-aware cross-checking, fail-closed on
     unresolved conflicts where the model requires the field. No scraping/provider
     integration yet.
  D. Simulation-driven MLB analysis: historical data → leakage-safe rates/features →
     calibrated PA probabilities → current matchup/context → MLB-specific simulation →
     market probabilities → sportsbook EV/ranking. Simulation only after probability
     quality is validated/calibrated.

═══════════════════════════════════════════════════════
CURRENT VERIFIED STATE
═══════════════════════════════════════════════════════

Version:        0.3.3
Project verified state (main): main is at merge commit dcab8bb (PR #50), based on
                Ryan's post-merge verification. main is synchronized with origin/main.
Tests passing:  2380

Current task branch: chore/update-handoffs-after-v0.3.3 — a temporary docs/ops
                handoff-update branch only, not the permanent verified project branch.
                This branch does not change the verified project state above, which
                reflects main after the v0.3.3 merge.

Preceding architecture chore: PR #42 — Legacy Pipeline Architecture Audit (merged,
                docs-only, no version bump) — merged immediately before v0.3.0.

Latest version PR:      PR #50 — v0.3.3 MLB Plate-Appearance Probability Baseline
                        — merged
                        https://github.com/Rryan0613/RSB/pull/50
Merge commit:           dcab8bb
Implementation commit:  72c4a8f
Feature branch:         feature/v0.3.3-mlb-pa-probability-baseline
                        (deleted locally and remotely)
PR #50 post-merge verification: complete — 2380 tests passed; GitHub CI passed

Latest completed version: v0.3.3 — MLB Plate-Appearance Probability Baseline
Next target:    v0.3.4 — MLB Walk-Forward Evaluation & Calibration, the next
                roadmap objective (directional). See docs/MLB_NBA_ROADMAP.md.
Next status:    Not started. Implementation architecture not yet approved — a separate
                v0.3.4 inspection/planning stage requires ChatGPT review and approval
                before coding begins. Do not create a new version branch until that
                plan is approved.

If strict verification is needed, ask the user to run:

cd /Users/ryan/Desktop/PersonalWebsite/RSB/worldcup_ai_v0_1
git branch --show-current
git status -sb
git status --short
.venv/bin/python -m pytest --tb=short -q

═══════════════════════════════════════════════════════
APPROVED ROADMAP
═══════════════════════════════════════════════════════

v0.2.1    Edge Calculation Primitives                                        ← COMPLETE
v0.2.2    Candidate Evaluation Record / Pass Reason Primitives               ← COMPLETE
v0.2.3    Backtest Review Overlay                                            ← COMPLETE
v0.2.4    Odds Expansion / EV Math Primitives                                ← COMPLETE
v0.2.5    Prop Market / Candidate Schema Primitives                          ← COMPLETE
v0.2.6    Odds Snapshot / Provider Record Normalization                      ← COMPLETE
v0.2.7    Prop Result / Settlement Primitives                                ← COMPLETE
v0.2.8    Candidate EV Enrichment                                            ← COMPLETE
v0.2.9    Candidate Ranking Primitives                                       ← COMPLETE
v0.2.10   Ranked Candidate Report Primitives                                 ← COMPLETE
v0.2.11   Sport / Market Capability Profile Primitives                      ← COMPLETE
v0.2.12   MLB Capability Profile Seed                                        ← COMPLETE
v0.3.0    Candidate Evaluation Contract                                      ← COMPLETE
v0.3.1    MLB Statcast Data Foundation                                       ← COMPLETE
v0.3.2    MLB Plate-Appearance Dataset & Rate Foundation                     ← COMPLETE
v0.3.3    MLB Plate-Appearance Probability Baseline                          ← COMPLETE

── Near-term v0.2.x ──────────────────────────────────────────

v0.2.6    Odds Snapshot / Provider Record Normalization                              ← COMPLETE
          src/odds_snapshot.py — pure snapshot/provider record normalization primitives.
          odds_found_at lives on the snapshot record, not the candidate record.
          CLV later compares odds_found_at to closing line.

v0.2.7    Prop Result / Settlement Primitives                                        ← COMPLETE
          src/prop_result.py — pure prop result / settlement record normalization
          primitives. Defines VALID_SETTLEMENT_STATUSES and FINAL_SETTLEMENT_STATUSES,
          normalize_market_type, normalize_selection, normalize_settlement_status, and
          build_prop_result. Settlement behavior is sportsbook-specific, sport-specific,
          and market-specific. build_prop_result enforces settled_at requirements by
          status, void_reason constraints, and bool validation for start_required and
          participation_required. No automatic grading or inference.

v0.2.8    Candidate EV Enrichment                                                     ← COMPLETE
          src/candidate_ev.py — pure in-memory candidate EV enrichment primitives.
          build_candidate_ev_enrichment(candidate, odds_snapshot, model_probability,
          minimum_edge=0.0, metadata=None) merges a candidate and an odds snapshot,
          validates matching identity fields and lines, computes implied_probability,
          edge, and expected_value, and attaches a candidate_evaluation built via
          candidate_evaluation.build_candidate_evaluation(). CRITICAL: v0.2.8 does NOT
          compute model_probability internally — it consumes a caller-supplied
          model_probability only. Full automated model-generated EV requires the
          v0.5.x sport-specific model layer.

v0.2.9    Candidate Ranking Primitives                                                ← COMPLETE
          src/candidate_ranking.py — pure in-memory candidate ranking primitives.
          CandidateRankingValidationError. rank_candidate_ev_enrichments(candidate_evs,
          *, data_quality_scores=None, confidence_scores=None, calibration_scores=None,
          metadata=None) ranks v0.2.8 candidate EV enrichment records by expected_value
          descending, edge descending, optional data_quality_score descending (None
          last), optional confidence_score descending (None last), optional
          calibration_score descending (None last), then original input index
          ascending. Ranked records get a 1-based rank. Records whose embedded
          candidate_evaluation status is rejected or not_evaluable are excluded from
          ranking and returned after ranked records with rank=None, preserving original
          input order. Still pure/in-memory. No provider ingestion, recommendations,
          picks, locks, parlays, or runtime wiring.

v0.2.10   Ranked Candidate Report Primitives                                          ← COMPLETE
          src/candidate_report.py — pure in-memory ranked candidate report primitives.
          CandidateReportValidationError. build_candidate_report(ranked_candidates, *,
          top_n=None, metadata=None) consumes v0.2.9 ranked candidate records and
          produces a report/audit dict with total_records, ranked_count,
          excluded_count, top_n, top_ranked_candidates, excluded_candidates,
          pass_reason_counts, ranking_status_counts, and metadata. Validates required
          ranked-record keys (rank, ranking_status, candidate_status, pass_reasons,
          original_index), enforces strict sequential positive rank for ranked records
          and rank=None for excluded records, and reuses
          candidate_evaluation.validate_pass_reasons(). Accepts empty input and returns
          a valid all-zero report. Still pure/in-memory. No provider ingestion,
          recommendations, picks, locks, parlays, file output, or runtime wiring.

v0.2.11   Sport / Market Capability Profile Primitives                               ← COMPLETE
          src/market_capability.py — pure in-memory sport/market capability profile
          primitives. MarketCapabilityValidationError. VALID_LINE_VALUE_TYPES =
          frozenset({"integer", "float"}). normalize_sport, normalize_league,
          normalize_market_type, and normalize_selection_type use open slug
          normalization. build_market_capability(...) returns a single market
          capability record (sport, league, market_type, selection_type, line
          semantics, requires_player/team/opponent/starting_status,
          start_required_supported, participation_required_supported,
          void_condition_supported, settlement_rule_required,
          supported_selection_values, required_fields, optional_fields, metadata).
          build_sport_market_profile(...) aggregates a list of market capability
          records into a per-sport/league profile with supported_markets,
          market_count, and metadata. Validates line semantics (requires_line implies
          allows_line; allows_line implies a valid line_value_type), rejects
          duplicate/overlapping required_fields and optional_fields, and rejects
          duplicate supported markets by (market_type, selection_type). Does not
          define closed sport/league/market/selection vocabularies yet. Still
          pure/in-memory. No provider ingestion, World Cup capability seed, other
          sport seeds, recommendations, picks, locks, parlays, file output, or
          runtime wiring.

v0.2.12   MLB Capability Profile Seed                                                ← COMPLETE
          src/mlb_capability.py — pure in-memory MLB capability profile seed built on
          the v0.2.11 sport/market capability profile primitives. MLB_SPORT =
          "baseball", MLB_LEAGUE = "mlb". build_mlb_capability_profile() calls
          market_capability.build_sport_market_profile() with 15 supported MLB market
          capability records: moneyline, run_line, total_runs, team_total_runs,
          player_hits, player_total_bases, player_home_runs, player_rbis, player_runs,
          player_stolen_bases, pitcher_strikeouts, pitcher_outs_recorded,
          pitcher_hits_allowed, pitcher_walks_allowed, and
          pitcher_earned_runs_allowed. Capability metadata only — no live provider
          integration, no odds ingestion, no predictive MLB modeling, and no runtime
          wiring.

v0.3.0    Candidate Evaluation Contract                                      ← COMPLETE
          src/candidate_evaluation.py — added validate_candidate_evaluation_record(),
          the canonical whole-record validator for the {status, edge, pass_reasons}
          shape. candidate_ranking.py refactored to delegate embedded-record validation
          to it. docs/CANDIDATE_EVALUATION_CONTRACT.md added as the architecture
          document. Merged as PR #43, merge commit 20f34ab, implementation commit
          151010b, 1938 tests passed. This is a different scope than the "Provider
          Source Contract / Identity Normalization Primitives" scope earlier handoffs
          predicted for v0.3.0 — see the v0.3.0 SUMMARY section below for full detail.

          Important roadmap note: provider batch normalization and provider
          coverage/staleness diagnostics remain delayed until after the sport/market
          capability profile layer exists. v0.2.11 created the generic primitives and
          v0.2.12 seeded the MLB capability profile. A World Cup capability profile
          seed and provider contracts remain future, unscoped work.

v0.3.1    MLB Statcast Data Foundation                                       ← COMPLETE
          Merged 2026-08-12, superseding the earlier Legacy Candidate Translation /
          Canonical Odds Snapshot Reconciliation / Candidate Persistence Framework /
          First MLB Runtime Skeleton sequence. PR #46, merge commit 3ae353f,
          implementation commit 765b5ed, 2156 tests passed. Feature branch deleted
          locally and remotely. See docs/MLB_NBA_ROADMAP.md.

v0.3.2    MLB Plate-Appearance Dataset & Rate Foundation                     ← COMPLETE
          Merged 2026-08-12. Derived leakage-safe plate-appearance records and prior
          batter/pitcher/league empirical outcome rates from v0.3.1's normalized
          pitch-level snapshots, per docs/MLB_PLATE_APPEARANCE_CONTRACT.md. PR #48,
          merge commit b876eb1, implementation commit 9212c93, real-Savant correction
          commit ba3ce68, 2307 tests passed. Feature branch deleted locally and
          remotely. See docs/MLB_NBA_ROADMAP.md and the v0.3.2 SUMMARY section below.

v0.3.3    MLB Plate-Appearance Probability Baseline                          ← COMPLETE
          Merged 2026-08-18. Turned v0.3.2's leakage-safe prior batter/pitcher/league
          outcome counts into one coherent categorical distribution over the 12
          RATE_CATEGORIES, per docs/MLB_PLATE_APPEARANCE_PROBABILITY_CONTRACT.md.
          PR #50, merge commit dcab8bb, implementation commit 72c4a8f, 2380 tests
          passed. Feature branch deleted locally and remotely. See
          docs/MLB_NBA_ROADMAP.md and the v0.3.3 SUMMARY section below.

v0.3.4    MLB Walk-Forward Evaluation & Calibration                      ← NEXT (DIRECTIONAL)
          Directional planning batch, not an independently approved implementation
          scope. v0.3.4 is the next roadmap objective but requires its own
          inspection/planning stage and ChatGPT approval before coding begins. Version
          numbers/titles may be reassessed after preceding versions teach us more. See
          docs/MLB_NBA_ROADMAP.md §10 for the considerations recorded during v0.3.3
          (intersection-sample comparison, scorable-target selection, provisional
          hyperparameters, matchup overconfidence, intentional_walk visibility).

v0.2.x    Controlled staking / Kelly primitives (possible, later)
          Kelly treated as stake-sizing math only, not recommendation logic.
          Only after EV and calibration are stronger.

── Mid-term v0.3.x ───────────────────────────────────────────

v0.3.x    Provider / Data Ingestion
          Integrate odds providers, sportsbook/player prop markets, normalized snapshots,
          provider coverage, staleness detection, and API constraints.
          Note: v0.3.x-v0.5.x are tightly coupled. Data, history, and modeling
          must develop together and cannot be fully sequential in practice.

── Historical data and calibration v0.4.x ────────────────────

v0.4.x    Historical Results / Closing Lines / CLV / Backtesting
          Track settled outcomes, closing prices, CLV, calibration buckets,
          market-specific performance, feature ablation support, and model review.
          CLV (closing line value) is a first-class proof-of-edge metric.
          If RSB consistently beats the closing line over a real sample, the model has
          genuine edge. If not, wins are likely variance.

── Sport-specific modeling v0.5.x ────────────────────────────

v0.5.x    Sport-Specific Prop Modeling + Research Context Layer
          Build probability models using sport-specific feature hierarchies.
          Convert injuries / news / lineup / research into structured model inputs.
          Learn and validate recency weighting rather than hardcoding it.
          Feature ablation: each feature must prove it improves Brier score, log loss,
          or CLV over enough data before staying in the model.

          Sport-modeling clarification:
          The sport-specific tier hierarchies below are illustrative examples, not
          exhaustive model specifications. Future v0.5.x sport models should organize
          their features into the following families:

            availability / role:         active/inactive status, projected start,
                                         expected minutes, snap share, route share,
                                         lineup confirmation, rotation role
            opportunity volume:          shots, touches, targets, carries, plate
                                         appearances, rebound chances, assist chances,
                                         shots on target, set-piece involvement
            efficiency / skill:          xG/shot, xA, usage efficiency, yards after
                                         catch, xwOBA, strikeout/walk profile,
                                         completion over expected
            matchup:                     opponent defensive scheme, pitcher/batter
                                         handedness, coverage type, rim protection,
                                         pressing style, park factors, weather where
                                         relevant
            game environment:            pace, spread, total, team implied total,
                                         rest/travel, altitude, tournament stage,
                                         game script, weather
            market context:              opening line, current line, closing line,
                                         sportsbook, odds movement, vig, limits,
                                         market availability
            data quality / uncertainty:  stale injury news, low sample size, role
                                         change, new coach or manager, tournament
                                         small-sample risk, provider coverage gaps
            calibration / feature ablation: features should earn their place by
                                         improving Brier score, log loss, CLV, or
                                         other validation metrics over meaningful
                                         samples before staying in the model

          These feature families are future v0.5.x roadmap context only. They are
          not v0.2.x implementation scope unless explicitly approved by ChatGPT.

          Basketball (NBA) tier hierarchy:
            Tier 1 (core): minutes projection, usage/role, shot volume, assist chances,
                           rebound chances, rotation stability, lineup/injury context
            Tier 2 (game env): spread, total, pace, team implied total, blowout risk,
                               rest/travel
            Tier 3 (refinement): pick-and-roll coverage, switching, rim protection,
                                 perimeter defense, foul tendencies, defensive scheme,
                                 transition frequency, play-type distribution
            Tier 4 (research): injuries, lineup news, coach quotes, beat reports,
                               rotation changes

          Soccer/World Cup tier hierarchy:
            Tier 1 (core): starting lineup confirmation (including void/start risk), xG,
                           shots on target, set-piece involvement, pressing role, tactical
                           position in attack, matchup style, opponent strength
            Schema must preserve support for void_condition / start_required to enable
            correct settlement and EV modeling for non-starting players.

          MLB: handedness, batting order, park factors, pitcher/batter matchups,
               bullpen usage, weather, platoon splits, recent role context.

          NFL: snap share, route participation, target share, red-zone usage,
               QB/scheme/coaching changes, weather, current-season role trends.

── Single candidate automation v0.6.x ───────────────────────

v0.6.x    Single Candidate Automation
          Automated scan for ranked +EV singles with explanations, guardrails, and
          pass/no-bet discipline.
          Long-term: must support multiple scan windows, not just one daily batch.
            Morning: general candidate identification, baseline odds snapshot
            Afternoon: lineup/injury update check, candidate refresh
            Pre-game (60-90 min out): final lineup confirmation, last-second line
                                      movement, final go/no-go
          Injury redistribution: when a key player is out, RSB should model teammate
          usage/minutes transfer from historical lineup-adjusted data. Markets lag on
          secondary player adjustments — this is a first-class edge opportunity.

── Parlay structural compatibility v0.7.x ────────────────────

v0.7.x    Parlay Structural Compatibility Primitives
          Detect structural parlay problems using rules/schema:
            - same player, same team, same game exposure
            - duplicate exposure
            - market overlap and contradiction
            - cannibalization (one leg's assumption invalidates another)
            - fragile shared game-script assumptions
          This is structural detection only — NOT empirical correlation estimation.
          Empirical correlation requires historical outcome data (v0.4.x-v0.5.x).
          Do not conflate structural compatibility with correlation modeling.

── Scenario-robust parlay builder v0.8.x ─────────────────────

v0.8.x    Scenario-Robust Parlay Builder
          Evaluate parlays across multiple plausible game environments.
          Optimize scenario-adjusted joint EV against sportsbook parlay price.
          A good parlay should not be dead solely because one fragile game script fails.
          Test each card against: close game, moderate favorite win, underdog competitive,
          high/low pace, foul trouble, late-game rotation changes.
          Correct correlation principle: optimize scenario-adjusted joint EV. Do not
          assume positive, negative, or independent correlation is automatically good.
          Same-game parlay books may already include correlation adjustments.

── Product / UI / app v0.9.x+ ────────────────────────────────

v0.9.x+   Product / UI / App Layer
          User-facing workflow: choose sport, date range, markets, sportsbooks.
          Generate ranked singles and constructed parlays.
          Explain decisions, track results, review performance.

Do not suggest pipeline wiring before it is explicitly scoped.
Do not suggest parlay logic before v0.7.x or explicit approval.
Do not suggest self-learning before v0.6.x or explicit approval.

═══════════════════════════════════════════════════════
EXTENDED PROJECT CONTEXT AND PRINCIPLES
═══════════════════════════════════════════════════════

RSB FINAL GOAL — SINGLES AND PARLAYS
─────────────────────────────────────────────────────

RSB's final product is a disciplined system for:

1. Finding ranked +EV single prop/pick candidates.
2. Rejecting weak, noisy, or no-edge singles.
3. Constructing analytically coherent parlays from compatible candidates.
4. Rejecting bad parlays even when individual legs look decent.
5. Explaining every candidate and parlay with reasoning, data quality, uncertainty,
   and pass conditions.
6. Tracking whether the model has genuine edge through CLV, calibration, and settled results.

PARLAY CONSTRUCTION PHILOSOPHY
─────────────────────────────────────────────────────

RSB must not bundle random +EV singles together and call it a parlay.

Parlay narrative trap to avoid:
  "Team A is better → blowout expected → Team A stars go over → Team B players go under."
  This dies if the game is close, pace is low, starters rest in Q4, foul trouble disrupts
  rotations, or the underdog keeps it competitive.

A game can go: close, one-sided, high pace, low pace, foul-heavy, rotation-heavy,
injury-affected, bench-heavy, or overtime.

A good parlay should not be dead solely because one simple game script fails.
The goal is not parlays immune to all outcomes — that is impossible.
The goal is to avoid cards where every leg depends on the same brittle assumption.

Parlay construction must eventually evaluate:
  individual leg EV and edge
  joint probability
  scenario-adjusted joint EV
  sportsbook parlay price
  same-game / same-player / same-team exposure
  market overlap, contradiction, cannibalization
  duplicate exposure to one player/team/game environment
  fragile shared assumptions
  data quality, calibration strength, line movement sensitivity
  CLV history where available

Correct correlation principle:
  Positive correlation usually increases joint hit probability vs. independent multiplication.
  Negative correlation usually decreases joint hit probability vs. independent multiplication.
  Neither direction is automatically good or bad.
  The target is scenario-adjusted joint EV vs. sportsbook parlay price.
  Same-game parlay books may already include correlation adjustments — RSB must not
  blindly chase correlation.

Structural compatibility vs. empirical correlation:
  Structural detection (same player, same team, same game, overlap, contradiction,
  cannibalization) can be done with rules/schema and belongs in v0.7.x.
  Empirical correlation estimation requires historical outcome data and belongs in
  the v0.4.x-v0.5.x historical/modeling layers. Do not conflate these two concepts.

CLV — CLOSING LINE VALUE
─────────────────────────────────────────────────────

CLV is a first-class long-term proof-of-edge metric.

If RSB consistently identifies candidates whose prices are better than the closing line
over a meaningful sample, the model has genuine edge. If not, wins are likely variance.

RSB should eventually track:
  found price / odds_found_at (on the odds snapshot record, not the candidate record)
  best available price
  closing price and closing line
  whether RSB beat the close
  CLV by market, sportsbook, model version, confidence bucket, data-quality bucket

TIMESTAMP SPLIT
─────────────────────────────────────────────────────

These are different timestamps — do not conflate them:

  created_at:         when RSB created the candidate record
                      Lives on the candidate schema (v0.2.5)

  odds_found_at /     when the specific price was observed from a provider/source
  odds_timestamp:     Lives on the odds snapshot record (v0.2.6)

CLV comparison requires odds_found_at, not created_at. If odds_found_at is not
captured from v0.2.6 onward, CLV calculations will be impossible to retrofit.

DATASET WINDOWS — SPORT-SPECIFIC
─────────────────────────────────────────────────────

Do not hard-code "last two seasons" as a universal rule.

NBA: 2 seasons is a reasonable starting window. Weight current season heavily.
     Role/team/coach changes override raw historical averages.

MLB: 2 seasons is reasonable given large sample sizes. Key context: park factors,
     handedness, batting order, pitcher/batter matchups, bullpen usage, weather.

NFL: 2 seasons can be thin (~34-38 games per team). Weight current season heavily.
     Coaching, QB, scheme, snap share, and target share changes often matter more
     than historical averages.

World Cup / Soccer: "Last two seasons" does not translate cleanly to international
     football. World Cup is every 4 years. Supplement with: qualifiers, UEFA Euros,
     Copa America, AFCON, Asian Cup, Nations League. Apply uncertainty bounds for
     small samples. Club form is relevant context.

Recency weighting should be learned and validated from calibration data, not
permanently hardcoded as fixed constants.

FEATURE HIERARCHY — GENERAL PRINCIPLE
─────────────────────────────────────────────────────

Tier 1 — Core projection drivers (most important):
  minutes / availability / usage / role / starting status / lineup context

Tier 2 — Game environment:
  spread, total, pace, team implied total, blowout risk, rest/travel, weather

Tier 3 — Matchup / tactical / play-type features (model refinement layer):
  pick-and-roll role/coverage, switching, set pieces, defensive scheme,
  play-type distribution, transition frequency, foul tendencies, rim protection

Tier 4 — Research / qualitative context (must be converted to structured inputs):
  injury reports, lineup news, coach quotes, beat reporter notes, rotation changes,
  load management, trade/roster moves, market movement

Tactical/play-type data matters and should be encoded as structured features, not
freeform narrative. It belongs in v0.5.x model refinement, not v0.2.5 schema.

Feature ablation principle:
  Features must earn their place. RSB should eventually test whether each feature
  improves Brier score, log loss, or CLV over a meaningful sample. Features that
  do not improve predictive quality should be demoted or removed.

INJURY REDISTRIBUTION MODELING
─────────────────────────────────────────────────────

When a key player is out, RSB should eventually model usage/minutes redistribution
to teammates using historical lineup-adjusted data.

Example: Brunson out → KAT usage increases, secondary guards take more assist chances,
Knicks offensive efficiency may drop, opponent defensive focus shifts.

Markets lag on secondary player adjustments — this is a first-class edge opportunity.
Must be data-validated, not treated as automatic narrative logic.

RESEARCH LAYER — REAL-TIME REQUIREMENT
─────────────────────────────────────────────────────

Injury news and lineup confirmation often arrive 60-90 minutes before game time.
A single daily batch is not the final automation architecture.

Long-term, RSB should support multiple scan windows:
  Morning: general candidate identification, baseline odds snapshot
  Afternoon: injury/lineup update check, candidate flag refresh
  Pre-game: final lineup confirmation, late line movement, final go/no-go

The pre-game window is the highest-value window for research-based adjustments.

SETTLEMENT BEHAVIOR — SPORT AND BOOK SPECIFIC
─────────────────────────────────────────────────────

Settlement behavior is sportsbook-specific, sport-specific, market-specific, and
sometimes parlay-format-specific. RSB must not assume universal settlement rules.

For player props, settlement varies significantly by book and market:
  Some books/markets require the player to start.
  Some require only participation (any appearance).
  Some void props if the player is inactive or does not start.
  Some settle once the player participates regardless of role.
  Soccer has especially important start/appearance/void-rule differences, but
  NBA/NFL/MLB/NHL also have book-specific participation, inactive, and early-exit rules.

The schema and settlement roadmap must preserve support for fields such as:
  start_required
  participation_required
  void_condition
  settlement_rule
  book-specific settlement metadata

v0.2.5 preserved metadata support where scoped. Full settlement primitives were implemented in v0.2.7.
RSB must not assume any universal settlement behavior across sports, books, or markets.

ACCOUNT HEALTH / EXECUTION CONSTRAINTS
─────────────────────────────────────────────────────

RSB can identify theoretical +EV opportunities, but execution depends on:
  which books the user can access
  whether limits or restrictions exist
  whether the best available line is actually placeable
  odds movement before placement
  book-specific max bet constraints

RSB should not assume every theoretical best price is executable.
This is a product constraint, not a technical one.

DATA BOTTLENECKS — WHAT IS ACTUALLY HARD
─────────────────────────────────────────────────────

The hardest parts of RSB are not EV math. EV math is already built. The hard parts are:
  clean historical player prop data with settlement
  settled prop outcomes over enough samples per market
  closing line history for CLV
  reliable real-time injury/lineup data
  sportsbook/player/team ID normalization across providers
  odds provider coverage and rate limits
  market availability differences by sport/book
  calibration over enough samples
  play-type/tactical datasets, which may be expensive or unavailable

RSB should not pretend it is calibrated or autonomous until the data layer supports
that claim with verified settled results.

═══════════════════════════════════════════════════════
RECENT COMPLETED PRs
═══════════════════════════════════════════════════════

PR #50:
v0.3.3 MLB Plate-Appearance Probability Baseline
https://github.com/Rryan0613/RSB/pull/50

PR #48:
v0.3.2 MLB Plate-Appearance Dataset & Rate Foundation
https://github.com/Rryan0613/RSB/pull/48

PR #46:
v0.3.1 MLB Statcast Data Foundation
https://github.com/Rryan0613/RSB/pull/46

PR #45:
Update roadmap for MLB-first development — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/45

PR #44:
Update handoffs after v0.3.0 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/44

PR #43:
v0.3.0 Candidate Evaluation Contract
https://github.com/Rryan0613/RSB/pull/43

PR #42:
Legacy Pipeline Architecture Audit — architecture/docs chore, not a version
https://github.com/Rryan0613/RSB/pull/42

PR #40:
v0.2.12 MLB Capability Profile Seed
https://github.com/Rryan0613/RSB/pull/40

PR #38:
v0.2.11 Sport Market Capability Profile Primitives
https://github.com/Rryan0613/RSB/pull/38

PR #36:
v0.2.10 Ranked Candidate Report Primitives
https://github.com/Rryan0613/RSB/pull/36

PR #34:
v0.2.9 Candidate Ranking Primitives
https://github.com/Rryan0613/RSB/pull/34

PR #33:
Update handoffs after v0.2.8 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/33

PR #32:
v0.2.8 Candidate EV Enrichment
https://github.com/Rryan0613/RSB/pull/32

PR #31:
Update handoffs after v0.2.7 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/31

PR #30:
v0.2.7 Prop Result Settlement Primitives
https://github.com/Rryan0613/RSB/pull/30

PR #29:
Update handoffs after v0.2.6 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/29

PR #28:
v0.2.6 Odds Snapshot Provider Records
https://github.com/Rryan0613/RSB/pull/28

PR #27:
Update handoffs after v0.2.5 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/27

PR #26:
v0.2.5 Prop Candidate Schema Primitives
https://github.com/Rryan0613/RSB/pull/26

PR #25:
Update RSB long-term handoff context — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/25

PR #24:
Update handoffs after v0.2.4 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/24

PR #23:
v0.2.4 Odds and EV Primitives
https://github.com/Rryan0613/RSB/pull/23

PR #22:
Update handoffs after v0.2.3 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/22

PR #21:
v0.2.3 Backtest Review Overlay
https://github.com/Rryan0613/RSB/pull/21

PR #20:
Clarify Claude CLI branch workflow — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/20

PR #19:
Update handoff files after PR #18 — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/19

PR #18:
Add project handoff files — docs/ops chore (not a version)
https://github.com/Rryan0613/RSB/pull/18

PR #17:
v0.2.2 Candidate Evaluation Record / Pass Reason Primitives
https://github.com/Rryan0613/RSB/pull/17

PR #16:
v0.2.1 Edge Calculation Primitives
https://github.com/Rryan0613/RSB/pull/16

PR #15:
v0.2.0 Odds / Implied Probability Primitives
https://github.com/Rryan0613/RSB/pull/15

PR #14:
v0.1.9.4 Prediction Review Note Primitives
https://github.com/Rryan0613/RSB/pull/14

PR #13:
v0.1.9.3 Prediction Review Taxonomy Primitives
https://github.com/Rryan0613/RSB/pull/13

PR #12:
v0.1.9.2 Tournament Stage and Market Semantics Validation
https://github.com/Rryan0613/RSB/pull/12

PR #11:
v0.1.9.1 4 derived ratio features
https://github.com/Rryan0613/RSB/pull/11

PR #10:
v0.1.9.0 audit cleanup, NaN/inf test coverage and skipped-row docs
https://github.com/Rryan0613/RSB/pull/10

PR #9:
v0.1.9.0 backtest report output, build_backtest_report
https://github.com/Rryan0613/RSB/pull/9

═══════════════════════════════════════════════════════
v0.3.3 SUMMARY — LATEST COMPLETED VERSION
═══════════════════════════════════════════════════════

v0.3.3 added the MLB plate-appearance probability baseline: it turns v0.3.2's
leakage-safe prior batter/pitcher/league outcome counts into one coherent categorical
probability distribution per plate appearance, per the new
docs/MLB_PLATE_APPEARANCE_PROBABILITY_CONTRACT.md.

Files added:

* docs/MLB_PLATE_APPEARANCE_PROBABILITY_CONTRACT.md
* src/mlb/plate_appearance_probability.py
* tests/test_plate_appearance_probability.py

Files modified:

* src/mlb/__init__.py

Behavior summary:

* Four selectable methods in MODEL_METHODS: league_only, batter_shrinkage,
  pitcher_shrinkage, matchup_combination — steps 1-4 of the transparent baseline
  progression the roadmap anticipated. Step 5 (an ML challenger) remains future work and
  is not scheduled.
* league_only is the league categorical baseline smoothed toward uniform:
  L[k] = (c_L[k] + alpha/K) / (n_L + alpha), K = len(RATE_CATEGORIES) = 12. alpha > 0
  guarantees strict positivity for every category, so there is no epsilon clamping
  anywhere in the module, and n_L == 0 reduces algebraically to uniform with no separate
  fallback branch.
* batter_shrinkage and pitcher_shrinkage share one entity-agnostic implementation
  (shrink_entity_distribution): the closed-form Dirichlet(kappa_e * L) posterior mean
  p_e[k] = (c_e[k] + kappa_e * L[k]) / (n_e + kappa_e). Normalization is automatic, not
  imposed. n_e == 0 yields exactly L, so unseen entities need no special case; tiny
  samples degrade smoothly and no minimum-sample threshold exists.
* matchup_combination is a multiplicative relative-likelihood combination
  r[k] = b[k] * q[k] / L[k], normalized, computed in log space with max-subtraction for
  numerical stability. It is equivalent to classical log5 in the binary case and is
  explicitly NOT claimed to be the uniquely canonical K-category generalization.
* One coherent distribution over the 12 RATE_CATEGORIES (reused verbatim from v0.3.2,
  never redefined). Every probability is finite, strictly positive, and <= 1; the sum is
  validated to 1 within 1e-9 and never silently renormalized.
* intentional_walk is its own categorical outcome, never merged into walk. Sparse IBB
  counts are handled by the same shrinkage as every other category — there is no bespoke
  IBB logic in the module.
* Leakage boundary is enforced by field set, not by trusting function bodies:
  _extract_prior_context strips each record to a fixed whitelist before any math runs.
  pa_status, pa_outcome_detailed, pa_outcome_category, and terminal_pa_event_raw are
  excluded — a predictor must never condition on how the PA resolved, nor on whether it
  resolved at all. prior_*_outcome_rates are also excluded, since this module re-derives
  smoothed rates from raw counts.
* Completion status is NOT a gate: an incomplete or truncated_pa PA still receives a
  well-defined pre-PA distribution. Selecting which records carry a scorable categorical
  target is v0.3.4 evaluation work.
* Pitcher attribution IS a gate: with pitcher_rate_eligible == False, pitcher_shrinkage
  and matchup_combination raise PitcherAttributionUnavailableError (a subclass of the
  module error, so a broad except still catches it). The terminal pitcher_id is also
  WITHHELD (emitted as None) on league_only/batter_shrinkage records for those PAs,
  because that identity was unknowable before the PA began. pitcher_rate_eligible is
  retained as retrospective metadata; supported_methods_for() lets callers select
  records up front instead of using exceptions for control flow.
* Provisional hyperparameters, deliberately NOT empirically optimized:
  league_prior_strength = 1.0, batter_prior_strength = 100.0,
  pitcher_prior_strength = 100.0. All three are explicit keyword arguments, and every
  output record carries the strengths actually used plus model_config_version, so two
  artifacts sharing a method label can never be silently incomparable.
* Output is a plain dict shaped by PROBABILITY_FIELD_ORDER, built by
  build_pa_probability_distribution (method is a required keyword argument with no
  default, so the most complex method can never be selected by accident) and checked by
  validate_pa_probability_distribution — the same build-and-validate pattern as v0.3.0
  and v0.3.2. Canonical key ordering is a builder guarantee for bit-reproducible
  serialization and float summation, not a condition of validity.
* Deterministic and pure: all iteration goes through the RATE_CATEGORIES tuple,
  math.fsum is used for probability sums, there is no shared or global state, and no
  function mutates its inputs.
* Package-root API is build_pa_probability_distribution, supported_methods_for, and
  validate_pa_probability_distribution, plus error types and model constants. The Level
  1-3 estimator functions stay importable from mlb.plate_appearance_probability but are
  deliberately NOT package-root API — they take a prior_context that only the private
  _extract_prior_context can legitimately produce.
* data/worldcup_ai.db, src/database.py, and the World Cup runtime are unchanged.

Tests:

* tests/test_plate_appearance_probability.py — 73 new probability-model tests
* Full local test result after merge: 2380 passed, 0 failed, 0 skipped
* GitHub CI passed

Implementation commit: 72c4a8f
Merge commit: dcab8bb (PR #50)

v0.3.3 intentionally did not add: walk-forward evaluation, Brier score, log loss, or
calibration; empirical tuning of the prior strengths; per-category prior strengths; a
persisted probability artifact or manifest; a PA-start-pitcher prior contract;
handedness/platoon splits; situational conditioning on base-out state, score, or inning;
park factors or opponent adjustment; an ML challenger model; simulation,
PA-opportunity/lineup modeling, or market projection; sportsbook odds/EV/ranking; MLB
runtime orchestration; a prediction-time (upcoming-PA) input contract; World Cup
changes; or NBA changes. See docs/MLB_PLATE_APPEARANCE_PROBABILITY_CONTRACT.md §10 and
docs/MLB_NBA_ROADMAP.md for the v0.3.4+ directional sequence.

Future points recorded during v0.3.3 (directional; none approved or scheduled):

* v0.3.4 model-vs-model comparison should use an intersection sample so competing
  methods share one evaluation denominator, since pitcher-dependent methods exclude
  pitcher_rate_eligible == False PAs. Coverage should be reported separately.
* The multiplicative matchup baseline can compound same-direction batter/pitcher
  deviations and become overconfident. Measure that through calibration rather than
  assuming it is good or bad.
* intentional_walk's highly situational nature should stay visible in outcome-level
  evaluation rather than being hidden by merging it with ordinary walks.
* A PA-start-pitcher prior contract could recover pitcher-dependent coverage for
  mid-PA-substitution PAs (source_pitcher_ids[0] already IS the PA-start pitcher; only
  the pitcher-prior keying is missing). It is a v0.3.2-LAYER contract change. Not
  scheduled.
* A prediction-time / upcoming-PA input contract is a named architectural gap between
  historical evaluation and operational prediction. Not scheduled.
* A PA-opportunity / lineup / game-sequencing model is required between per-PA
  probabilities and player game-level markets (hits, total bases, home runs). Not
  scheduled.
* Handedness/platoon splits are a strong future challenger-feature candidate, but are
  NOT assigned to v0.3.4 or any other version.

═══════════════════════════════════════════════════════
v0.3.2 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.3.2 added a leakage-safe MLB plate-appearance (PA) dataset and prior batter/pitcher/
league empirical rate foundation, derived from v0.3.1's normalized pitch-level Statcast
records, per the new docs/MLB_PLATE_APPEARANCE_CONTRACT.md.

Files added:

* docs/MLB_PLATE_APPEARANCE_CONTRACT.md
* src/mlb/plate_appearance.py
* src/mlb/plate_appearance_rates.py
* src/mlb/plate_appearance_snapshot.py
* tests/test_plate_appearance.py
* tests/test_plate_appearance_rates.py
* tests/test_plate_appearance_snapshot.py

Files modified:

* src/mlb/__init__.py
* src/paths.py
* tests/test_paths.py

Behavior summary:

* plate_appearance.py groups pitches into one PA per (source_game_id, at_bat_number),
  with a deterministic rsb_pa_id and contiguous pitch_number chronology validation
  (fail-closed on gaps, duplicates, or batter/game-context/provenance mismatches).
* Chronology-first terminal-pitch detection: the max-pitch_number pitch is the only
  terminal candidate; a non-null pa_event_raw on any earlier pitch fails closed.
* Closed detailed/category outcome taxonomy for completed PAs, with intentional_walk
  kept distinct from ordinary walk at every layer. A null terminal event yields
  pa_status = "incomplete" with no outcome classification.
* Real-Savant correction (commit ba3ce68): a terminal pa_event_raw of "truncated_pa"
  (INCOMPLETE_TERMINAL_EVENTS) is recognized as pa_status = "incomplete" — never a
  completed taxonomy/rate category — with the raw value preserved in
  terminal_pa_event_raw for provenance. Any other unrecognized non-null terminal value
  still fails closed.
* Forward-only mid-PA pitcher substitutions (e.g. A,A,B,B) are preserved, not rejected,
  with pitcher_rate_eligible = False and every distinct pitcher recorded in
  source_pitcher_ids; a sequence reverting to an earlier pitcher fails closed.
* plate_appearance_rates.py's attach_prior_outcome_rates() attaches leakage-safe
  batter/pitcher/league prior counts and raw empirical rates (rate is None, never 0.0,
  when the prior count is 0) to every PA. Only completed PAs update the running
  counters; the pitcher counter updates only when pitcher_rate_eligible == True.
  Batter/league counters can still update from a completed multi-pitcher PA.
* Same-day cross-game leakage policy: an earlier PA counts as prior only if its
  game_date is strictly earlier, or it shares the same source_game_id with a smaller
  at_bat_number — two different games on the same date never inform each other's
  prior_* state.
* plate_appearance_snapshot.py's create_plate_appearance_dataset() validates strict
  single-source-snapshot provenance (manifest key set, row count, re-serialized content
  hash, per-record snapshot_id/source_provider/normalized_schema_version agreement)
  before deriving anything, and produces a deterministic, content-derived
  derived_dataset_id (not timestamp-salted).
* No predictive/calibrated probabilities, shrinkage, ML, simulation, sportsbook
  odds/EV/ranking, or MLB runtime wiring — that begins at v0.3.3.
* data/worldcup_ai.db, src/database.py, and the World Cup runtime are unchanged.

Real-Savant validation summary: a representative manual detailed-pitch Baseball Savant
CSV was used as validation evidence before merge (not committed; not an RSB dataset
artifact). Representative, not exhaustive, observations: no non-null events on
non-terminal pitches in the sample; intent_walk observed; truncated_pa observed as an
incomplete/interrupted PA marker; null-terminal incomplete PAs also observed;
provider/export result-boundary behavior produced partial PA pitch sequences in the
oldest included data, and current fail-closed chronology validation correctly rejected
those as intended. Does not claim every future/historical Savant event code is known;
does not claim to establish why Baseball Savant emits truncated_pa.

Tests:

* tests/test_plate_appearance.py, tests/test_plate_appearance_rates.py,
  tests/test_plate_appearance_snapshot.py, plus tests/test_paths.py additions
* Full local test result after merge: 2307 passed, 0 failed, 0 skipped

Implementation commit: 9212c93
Real-Savant correction commit: ba3ce68
Merge commit: b876eb1 (PR #48)

v0.3.2 intentionally did not add: predictive/calibrated probabilities, shrinkage or
Bayesian priors, ML, simulation, sportsbook odds/EV/ranking/parlays, MLB
runtime/orchestration, automated Baseball Savant network ingestion, PostgreSQL/object
storage, multi-source reconciliation, World Cup changes, NBA changes, handedness-split
or park/opponent-adjusted rates, or a minimum-sample betting policy. See
docs/MLB_PLATE_APPEARANCE_CONTRACT.md §14 and docs/MLB_NBA_ROADMAP.md for the v0.3.3+
directional sequence.

═══════════════════════════════════════════════════════
v0.3.1 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.3.1 added a manual-CSV-only historical MLB Statcast data foundation: an RSB-owned
normalized pitch-level contract and immutable raw/normalized/manifest snapshots with
SHA-256 provenance. No automated Baseball Savant/MLB access.

Files added:

* src/mlb/__init__.py
* src/mlb/statcast_import.py
* src/mlb/statcast_normalize.py
* src/mlb/statcast_snapshot.py
* tests/test_statcast_import.py
* tests/test_statcast_normalize.py
* tests/test_statcast_snapshot.py
* tests/fixtures/mlb/statcast_sample.csv

Files modified:

* src/paths.py (MLB data root: get_mlb_data_dir(), RSB_MLB_DATA_DIR override, and
  get_mlb_manual_input_dir()/get_mlb_raw_statcast_dir()/
  get_mlb_normalized_statcast_pitch_dir()/get_mlb_snapshot_dir() subdir helpers)
* tests/test_paths.py (MLB path-override tests)
* .gitignore (data/mlb/ added; no real historical MLB data committed)
* CHANGELOG.md

Behavior summary:

* statcast_import.py reads a local CSV export and validates file existence/
  readability/UTF-8/header structure; no baseball-semantic normalization.
* statcast_normalize.py builds the RSB-owned normalized pitch-level contract, fail-closed
  on missing/malformed required fields; provider-nullable fields normalize to None.
  rsb_pitch_id is a deterministic SHA-256 of (source_game_id, at_bat_number,
  pitch_number), validated unique per snapshot.
* statcast_snapshot.py orchestrates import → normalize → persist, writing three immutable
  artifacts per snapshot (raw .csv.gz, normalized .jsonl.gz, .manifest.json with SHA-256
  content hashes and full provenance). Existing snapshot artifacts are never overwritten.
* Zero new dependencies. CSV parsing uses the Python standard library (csv), not pandas.
* No plate-appearance aggregation, terminal-PA taxonomy, batter/pitcher rates, league
  priors, feature engineering, probability generation, modeling, calibration,
  simulation, sportsbook odds, candidate EV/ranking, or MLB runtime wiring — that begins
  at v0.3.2.
* data/worldcup_ai.db, src/database.py, and the World Cup runtime are unchanged.
* AST-based architecture isolation verified: new MLB modules do not import run_slate,
  database, data_quality, or market_selector.

Tests:

* 218 new tests across tests/test_statcast_import.py, tests/test_statcast_normalize.py,
  tests/test_statcast_snapshot.py, plus MLB path-override tests in tests/test_paths.py
* Full local test result after merge: 2156 passed (1938 baseline + 218 new), 0 failed,
  0 skipped

Implementation commit: 765b5ed
Merge commit: 3ae353f (PR #46)

v0.3.1 intentionally did not add: automated Baseball Savant/MLB access, PyBaseball or
any new dependency, plate-appearance aggregation, a terminal-PA outcome taxonomy,
batter/pitcher/league rate construction, feature engineering, probability generation,
modeling, calibration, simulation, sportsbook odds work, candidate EV/ranking bridging,
or MLB runtime orchestration. See docs/MLB_NBA_ROADMAP.md for the v0.3.2+ directional
sequence.

═══════════════════════════════════════════════════════
v0.3.0 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.3.0 added the canonical whole-record validator for the Candidate Evaluation Contract
and refactored candidate_ranking.py to delegate to it.

Files added:

* docs/CANDIDATE_EVALUATION_CONTRACT.md

Files modified:

* src/candidate_evaluation.py
* src/candidate_ranking.py
* tests/test_candidate_evaluation.py
* tests/test_candidate_ranking.py
* CHANGELOG.md
* pyproject.toml

Implemented in src/candidate_evaluation.py:

* validate_candidate_evaluation_record(value) -> dict — the canonical public whole-record
  validator for the {status, edge, pass_reasons} shape. Requires a dict with exactly these
  three keys; rejects non-dict input, missing keys, and unexpected/extra keys. Delegates
  all normalization and invariant enforcement to build_candidate_evaluation() — no
  independent reimplementation. Returns a freshly constructed, normalized dict, isolated
  from caller mutation.

Implemented in src/candidate_ranking.py:

* Delegates embedded candidate_evaluation record validation to
  validate_candidate_evaluation_record() instead of a private, duplicated edge-range
  validator. Catches CandidateEvaluationValidationError from the canonical validator and
  re-raises CandidateRankingValidationError (via raise ... from exc, preserving
  __cause__), identifying the offending candidate_evs[index] record. All existing output
  shape, sort order, and successful-path behavior are unchanged.

Behavior summary:

* No changes to build_candidate_evaluation, normalize_candidate_status,
  normalize_pass_reason, validate_pass_reasons, build_candidate_ev_enrichment, or any
  existing output shape/key order.
* No legacy runtime wiring, no persistence/database changes, no new
  aggregate/protocol/dataclass/TypedDict.
* candidate_report.py is unchanged — it validates flattened report fields, not a complete
  embedded candidate_evaluation record, so it is out of scope for this delegation.

Tests:

* tests/test_candidate_evaluation.py, tests/test_candidate_ranking.py
* Full local test result after merge: 1938 passed

Implementation commit: 151010b
Merge commit: 20f34ab (PR #43)

v0.3.0 intentionally did not add: a CanonicalCandidate aggregate, a new EV-enrichment
aggregate, protocols/abstract interfaces, dataclasses, TypedDict migrations, new
dependencies, legacy World Cup translation or vocabulary mapping, new legacy-specific
pass reasons, imports from legacy modules into the pure pipeline, MLB runtime
orchestration, provider ingestion, database/schema changes, persistence repositories,
candidate or database IDs, a model-version registry, settlement-rule execution,
result-ingestion wiring, post-event review wiring, CLV calculation, recommendations,
picks, locks, or parlays, staking/Kelly calculations, or any API/UI behavior.

═══════════════════════════════════════════════════════
v0.2.12 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.12 added a pure in-memory MLB capability profile seed built on the v0.2.11
sport/market capability profile primitives.

Files added:

* src/mlb_capability.py
* tests/test_mlb_capability.py

Implemented in src/mlb_capability.py:

* MLB_SPORT = "baseball"
* MLB_LEAGUE = "mlb"
* build_mlb_capability_profile() -> dict

Behavior summary:

* Pure in-memory capability metadata only; no live provider integration, no odds
  ingestion, no predictive MLB modeling, and no runtime wiring.
* build_mlb_capability_profile() calls market_capability.build_sport_market_profile()
  with sport="baseball", league="mlb", and 15 supported market capability records
  built via market_capability.build_market_capability().
* Declares 15 supported MLB markets:
  * moneyline
  * run_line
  * total_runs
  * team_total_runs
  * player_hits
  * player_total_bases
  * player_home_runs
  * player_rbis
  * player_runs
  * player_stolen_bases
  * pitcher_strikeouts
  * pitcher_outs_recorded
  * pitcher_hits_allowed
  * pitcher_walks_allowed
  * pitcher_earned_runs_allowed
* Game markets (moneyline/run_line/total_runs/team_total_runs) require team and
  opponent, not player, and do not support starting-status/void/participation fields.
* Batter markets (player_hits/player_total_bases/player_home_runs/player_rbis/
  player_runs/player_stolen_bases) require player, team, and opponent; support
  participation_required and void_condition; do not require starting status.
* Pitcher markets (pitcher_strikeouts/pitcher_outs_recorded/pitcher_hits_allowed/
  pitcher_walks_allowed/pitcher_earned_runs_allowed) require player, team, and
  opponent; require starting status; support start_required, participation_required,
  and void_condition.
* All market records carry metadata={"profile_type": "mlb_capability_seed"} at the
  profile level.
* No closed sport/league/market vocabulary beyond what v0.2.11 already defines.

Tests:

* tests/test_mlb_capability.py
* Full local test result after merge: 1897 passed

v0.2.12 intentionally did not add:

* live provider integration
* odds ingestion
* predictive MLB modeling
* runtime wiring
* other sport capability seeds (NFL/NBA/World Cup)
* provider source contracts
* provider batch normalization
* provider coverage/staleness diagnostics
* recommendations
* picks
* locks
* parlays
* staking/Kelly/bankroll
* frontend/API/UI
* database/schema changes
* file output
* README/CHANGELOG/config/pyproject/data changes

Important scope clarification:
The v0.2.12 omissions are version-scoped only. RSB's long-term product goal still
includes provider ingestion, sportsbook APIs, player prop data, simulations,
injury/lineup data, historical results, model calibration, and eventually disciplined
parlay construction. v0.2.12 avoided those items only because it was limited to a pure
MLB capability profile seed.

═══════════════════════════════════════════════════════
v0.2.11 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.11 added pure in-memory sport/market capability profile primitives.

Files added:

* src/market_capability.py
* tests/test_market_capability.py

Implemented in src/market_capability.py:

* MarketCapabilityValidationError(ValueError)
* VALID_LINE_VALUE_TYPES = frozenset({"integer", "float"})
* normalize_sport(sport) -> str
* normalize_league(league) -> str
* normalize_market_type(market_type) -> str
* normalize_selection_type(selection_type) -> str
* build_market_capability(...) -> dict
* build_sport_market_profile(...) -> dict

Internal/private implementation detail:

* _MARKET_CAPABILITY_KEYS defines the exact expected market capability record shape
  for profile validation.

build_market_capability() return shape:

{
    "sport":                              str,
    "league":                             str,
    "market_type":                        str,
    "selection_type":                     str,
    "requires_line":                      bool,
    "allows_line":                        bool,
    "line_value_type":                    str | None,
    "requires_player":                    bool,
    "requires_team":                      bool,
    "requires_opponent":                  bool,
    "requires_starting_status":           bool,
    "start_required_supported":           bool,
    "participation_required_supported":   bool,
    "void_condition_supported":           bool,
    "settlement_rule_required":           bool,
    "supported_selection_values":         list[str] | None,
    "required_fields":                    list[str],
    "optional_fields":                    list[str],
    "metadata":                           dict,
}

build_sport_market_profile() return shape:

{
    "sport":             str,
    "league":            str,
    "supported_markets": list[dict],
    "market_count":      int,
    "metadata":          dict,
}

Behavior summary:

* Pure in-memory capability/profile primitives only.
* Uses open slug normalization for sport, league, market_type, selection_type,
  supported_selection_values, required_fields, and optional_fields.
* Normalization strips whitespace, lowercases, replaces spaces with underscores, and
  replaces hyphens with underscores.
* Rejects non-string and empty/whitespace-only slug values.
* Does not define closed sport, league, market, or selection vocabularies yet.
* Validates strict bool fields for all capability flags.
* Validates line semantics:
  * requires_line=True requires allows_line=True
  * allows_line=False requires line_value_type=None
  * allows_line=True requires line_value_type to be provided
  * provided line_value_type must normalize to "integer" or "float"
* supported_selection_values may be None when values are not enumerated yet.
* Provided supported_selection_values must be non-empty list/tuple of unique
  normalized strings.
* required_fields and optional_fields default to [].
* required_fields and optional_fields must be list/tuple values when provided.
* Duplicate normalized required/optional fields are rejected.
* Overlap between required_fields and optional_fields is rejected.
* metadata defaults to {}, must be dict if provided, and is shallow-copied.
* build_sport_market_profile() accepts supported_markets=None and empty
  supported_markets.
* supported_markets must be list/tuple when provided.
* Each supported market must be a dict with the exact approved market capability key
  set.
* Missing supported-market keys raise MarketCapabilityValidationError.
* Extra supported-market keys raise MarketCapabilityValidationError.
* Supported market sport/league must match the profile sport/league.
* Duplicate supported markets by (market_type, selection_type) are rejected.
* Supported market dicts are shallow-copied.
* market_count equals len(supported_markets).
* Output is deterministic for identical input.

Tests:

* tests/test_market_capability.py
* Full local test result after merge: 1771 passed

v0.2.11 intentionally did not add:

* World Cup capability seed
* MLB/NFL/NBA capability seeds
* provider source contracts
* provider batch normalization
* provider coverage/staleness diagnostics
* live odds
* scraping
* sportsbook API integration
* database/schema changes
* file output
* runtime wiring
* recommendations
* picks
* locks
* parlays
* staking/Kelly/bankroll
* frontend/API/UI
* model probabilities
* simulations
* handoff updates during the feature branch
* README/CHANGELOG/config/pyproject/data changes

Important scope clarification:
The v0.2.11 omissions are version-scoped only. RSB's long-term product goal still
includes provider ingestion, sportsbook APIs, player prop data, simulations,
injury/lineup data, historical results, model calibration, and eventually disciplined
parlay construction. v0.2.11 avoided those items only because it was limited to pure
sport/market capability profile primitives.

═══════════════════════════════════════════════════════
v0.2.10 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.10 added pure in-memory ranked candidate report primitives.

Files added:

* src/candidate_report.py
* tests/test_candidate_report.py

Implemented in src/candidate_report.py:

* CandidateReportValidationError(ValueError)
* build_candidate_report(ranked_candidates, *, top_n=None, metadata=None) -> dict

Return shape:

{
    "total_records":         int,
    "ranked_count":          int,
    "excluded_count":        int,
    "top_n":                 int | None,
    "top_ranked_candidates": list[dict],
    "excluded_candidates":   list[dict],
    "pass_reason_counts":    dict[str, int],
    "ranking_status_counts": dict[str, int],
    "metadata":              dict,
}

Behavior summary:

* Pure in-memory report/audit primitive only
* Consumes ranked candidate records produced by candidate_ranking.rank_candidate_ev_enrichments()
* Accepts list or tuple input
* Accepts empty input and returns a valid all-zero report
* Validates required ranked-record keys: rank, ranking_status, candidate_status, pass_reasons, original_index
* ranking_status must be exactly "ranked" or "excluded"
* ranked records require positive non-bool integer rank in strict sequential order
* excluded records require rank=None
* Reuses candidate_evaluation.validate_pass_reasons()
* top_n, if provided, must be a non-bool integer >= 0
* metadata must be a dict if provided and is shallow-copied
* Output candidate records are shallow copies
* top_ranked_candidates contains ranked records only and applies top_n
* excluded_candidates contains excluded records only
* pass_reason_counts counts validated pass reasons
* ranking_status_counts counts ranked/excluded records

Tests:

* tests/test_candidate_report.py
* Full local test result after merge: 1614 passed

v0.2.10 intentionally did not add:

* provider ingestion
* live odds
* scraping
* database/schema changes
* file output
* runtime wiring
* recommendations
* picks
* locks
* parlays
* staking/Kelly/bankroll
* frontend/API/UI
* sport/market capability profiles
* World Cup capability seed
* provider batch normalization
* provider coverage/staleness diagnostics
* model probabilities or simulations
* data/input changes
* data/output changes
* data/worldcup_ai.db changes

Important scope clarification:
The v0.2.10 omissions are version-scoped only. RSB's long-term product goal still includes
provider ingestion, sportsbook APIs, player prop data, simulations, injury/lineup data,
historical results, model calibration, and eventually disciplined parlay construction.
v0.2.10 avoided those items only because it was limited to pure ranked candidate report
primitives.

═══════════════════════════════════════════════════════
v0.2.9 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.9 added pure in-memory candidate ranking primitives.

New files:

* src/candidate_ranking.py
* tests/test_candidate_ranking.py

Implemented in src/candidate_ranking.py:

* CandidateRankingValidationError(ValueError)
* rank_candidate_ev_enrichments(candidate_evs, *, data_quality_scores=None, confidence_scores=None, calibration_scores=None, metadata=None) -> list[dict]

Behavior summary:

* Pure in-memory candidate ranking only, no runtime wiring
* Ranks v0.2.8 candidate EV enrichment records by, in order:
  1. expected_value descending
  2. edge descending
  3. data_quality_score descending, None last
  4. confidence_score descending, None last
  5. calibration_score descending, None last
  6. original input index ascending
* Ranked records receive a 1-based rank
* Records whose embedded candidate_evaluation status is "rejected" or "not_evaluable"
  are excluded from ranking and returned after ranked records with rank=None
* Excluded records preserve original input order
* Validates candidate EV record shape, line, edge, expected_value, optional
  data_quality_scores/confidence_scores/calibration_scores lists, embedded
  candidate_evaluation, and metadata
* Function-level metadata is copied independently onto each ranking output record
* No rounding
* No runtime wiring

Tests:

* tests/test_candidate_ranking.py
* Full local test result after merge: 1564 passed

v0.2.9 intentionally did not add:

* provider ingestion
* live odds
* scraping
* database/schema changes
* runtime wiring
* recommendations
* picks
* locks
* parlays
* staking/Kelly/bankroll
* frontend/API/UI
* new dependencies
* CI changes
* data/input changes
* data/output changes
* data/worldcup_ai.db changes

Important scope clarification:
The v0.2.9 omissions are version-scoped only. RSB's long-term product goal still includes
provider ingestion, sportsbook APIs, player prop data, simulations, injury/lineup data,
historical results, model calibration, and eventually disciplined parlay construction.
v0.2.9 avoided those items only because it was limited to pure candidate ranking primitives.

═══════════════════════════════════════════════════════
v0.2.8 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.8 added pure in-memory candidate EV enrichment primitives.

Files added:

* src/candidate_ev.py
* tests/test_candidate_ev.py

Files modified:

* CHANGELOG.md
* CLAUDE.md
* README.md
* config/model_config.json
* pyproject.toml
* tests/test_paths.py

Implemented in src/candidate_ev.py:

* CandidateEVValidationError(ValueError)
* build_candidate_ev_enrichment(*, candidate, odds_snapshot, model_probability, minimum_edge=0.0, metadata=None) -> dict

Approved imports used:

* math
* candidate_evaluation.build_candidate_evaluation
* edge.calculate_edge
* ev.calculate_expected_value
* odds.american_to_decimal_odds
* odds.american_to_implied_probability
* odds.decimal_to_implied_probability
* odds.validate_probability

Return shape, always exactly 13 keys:

{
    "candidate":            dict,
    "odds_snapshot":        dict,
    "event_id":             str,
    "market_type":          str,
    "selection":            str,
    "line":                 float | None,
    "model_probability":    float,
    "implied_probability":  float,
    "edge":                 float,
    "expected_value":       float,
    "minimum_edge":         float,
    "candidate_evaluation": dict,
    "metadata":             dict,
}

Validation and behavior summary:

* candidate is required and must be dict
* candidate must contain event_id, market_type, selection, and line
* odds_snapshot is required and must be dict
* odds_snapshot must contain event_id, market_type, selection, line, odds, and odds_format
* candidate and odds_snapshot are shallow copied into the returned record
* caller inputs are not mutated
* candidate and odds_snapshot event_id must match exactly
* candidate and odds_snapshot market_type must match exactly
* candidate and odds_snapshot selection must match exactly
* identity fields are not normalized in candidate_ev.py; they should already be normalized by earlier primitives
* candidate line and odds_snapshot line are locally validated
* line may be int, float, or None
* line rejects bool, non-numeric, NaN, and infinity
* if both lines are not None, they must match after float conversion
* returned line is candidate line if present, otherwise odds_snapshot line if present, otherwise None
* model_probability is required and caller-supplied
* model_probability is validated by odds.validate_probability()
* model_probability is never generated, estimated, inferred, fetched, or modeled internally
* odds_format supports american and decimal only
* unsupported odds_format raises CandidateEVValidationError
* american odds path uses odds.american_to_implied_probability() and odds.american_to_decimal_odds()
* decimal odds path uses odds.decimal_to_implied_probability() and decimal odds directly for EV
* implied_probability is calculated from odds and odds_format
* edge is calculated with edge.calculate_edge(model_probability, implied_probability)
* expected_value is calculated with ev.calculate_expected_value(model_probability, decimal_odds)
* minimum_edge is caller-supplied, defaults to 0.0, must be numeric, non-bool, finite, and within [0.0, 1.0]
* minimum_edge is not read from config/model_config.json
* candidate_evaluation is built with candidate_evaluation.build_candidate_evaluation()
* if edge >= minimum_edge, candidate_evaluation status is "candidate" with pass_reasons []
* if edge < minimum_edge, candidate_evaluation status is "rejected" with pass_reasons ["edge_below_minimum"]
* metadata defaults to {}, must be dict if provided, and is shallow copied
* returns a plain dict
* no rounding
* no runtime wiring

Tests:

* tests/test_candidate_ev.py
* 135 new tests
* Full local test result after merge: 1444 passed
* GitHub Actions/checks for PR #32: 8 successful, 0 failing, 0 pending

v0.2.8 intentionally did not add:

* internal model_probability computation
* model/simulation changes
* sport-specific feature engineering
* v0.5.x sport model feature families as implementation
* provider ingestion
* live odds
* scraping
* database schema changes
* runtime wiring
* prop result / settlement usage
* CLV calculation
* historical result ingestion
* candidate ranking
* recommendations
* picks
* locks
* parlays
* staking/Kelly/bankroll
* frontend/API/UI
* new dependencies
* CI changes
* data/input changes
* data/output changes
* data/worldcup_ai.db changes
* handoff file updates during feature implementation

Important scope clarification:
The v0.2.8 omissions are version-scoped only. RSB's long-term product goal still includes
provider ingestion, sportsbook APIs, player prop data, simulations, injury/lineup data,
historical results, model calibration, ranked +EV prop/pick candidates, and eventually
disciplined parlay construction. v0.2.8 avoided those items only because it was limited to
pure candidate EV enrichment with caller-supplied model_probability.

═══════════════════════════════════════════════════════
v0.2.7 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.7 added pure prop result / settlement record normalization primitives.

New files:

* src/prop_result.py
* tests/test_prop_result.py

Implemented in src/prop_result.py:

* PropResultValidationError(ValueError)
* VALID_SETTLEMENT_STATUSES = frozenset({"won", "lost", "push", "void", "pending", "unknown"})
* FINAL_SETTLEMENT_STATUSES = frozenset({"won", "lost", "push", "void"})
* normalize_market_type(market_type)
* normalize_selection(selection)
* normalize_settlement_status(status)
* build_prop_result(...) -> dict

Return shape (always exactly 14 keys):

{
    "event_id":               str,
    "market_type":            str,
    "selection":              str,
    "line":                   float | None,
    "actual_value":           float | None,
    "settlement_status":      str,
    "settled_at":             str | None,
    "source":                 str | None,
    "settlement_rule":        str | None,
    "start_required":         bool | None,
    "participation_required": bool | None,
    "void_condition":         str | None,
    "void_reason":            str | None,
    "metadata":               dict,
}

Validation summary:

* event_id is a required stripped string; case is preserved
* market_type and selection are required slug-normalized strings
* line is optional numeric; rejects bool, non-numeric, NaN, and infinity; returns float
* actual_value is optional numeric; rejects bool, non-numeric, NaN, and infinity; returns float
* settlement_status is required, slug-normalized, and must be one of VALID_SETTLEMENT_STATUSES
* settled_at is an optional stripped string or None; no datetime parsing
* Final statuses (won, lost, push, void) require settled_at to be present
* Pending and unknown statuses require settled_at to be None
* source is an optional stripped string or None
* settlement_rule is an optional stripped string or None
* start_required is an optional bool or None; rejects non-bool when not None
* participation_required is an optional bool or None; rejects non-bool when not None
* void_condition is an optional stripped string or None
* void_reason is an optional stripped string or None
* void_reason is only allowed when settlement_status is "void"
* settlement_status "void" may have void_reason of None because providers may not expose a reason
* metadata defaults to {}, must be a dict, and is shallow copied
* No automatic grading or inference

Tests:

* tests/test_prop_result.py
* Full local test result after merge: 1309 passed
* GitHub Actions/checks for PR #30: 8 successful, 0 failing, 0 pending

v0.2.7 intentionally did not add:

* sportsbook settlement rule engine
* stat-based over/under grading
* automatic win/loss/push/void inference
* provider ingestion
* live odds
* scraping
* database schema changes
* runtime wiring
* prop candidate attachment or enrichment
* implied probability
* edge
* EV
* CLV
* calibration
* historical result ingestion
* ranking
* recommendations
* picks
* locks
* parlays
* frontend, API, or UI
* new dependencies
* CI changes
* data/input changes
* data/output changes
* data/worldcup_ai.db changes

Important scope clarification:
The v0.2.7 omissions are version-scoped only. RSB's long-term product goal still includes
odds providers, sportsbook APIs, player prop data, simulations, injury/lineup data, historical
results, model calibration, and eventually ranked +EV prop/pick candidates. v0.2.7 avoided
those items only because it was limited to pure prop result / settlement record normalization primitives.

═══════════════════════════════════════════════════════
v0.2.6 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.6 added pure odds snapshot / provider record normalization primitives.

New files:

* src/odds_snapshot.py
* tests/test_odds_snapshot.py

Implemented in src/odds_snapshot.py:

* OddsSnapshotValidationError(ValueError)
* VALID_ODDS_FORMATS = frozenset({"american", "decimal"})
* normalize_provider(provider)
* normalize_sportsbook(sportsbook)
* normalize_market_type(market_type)
* normalize_selection(selection)
* normalize_odds_format(odds_format)
* build_odds_snapshot(...) -> dict

Return shape (always exactly 11 keys):

{
    "provider":      str,
    "sportsbook":    str,
    "event_id":      str,
    "market_type":   str,
    "selection":     str,
    "line":          float | None,
    "odds":          float,
    "odds_format":   str,
    "odds_found_at": str,
    "source":        str | None,
    "metadata":      dict,
}

Validation summary:

* provider/sportsbook/market_type/selection are required slug-normalized strings
* event_id is required stripped string, case preserved
* line is optional numeric; rejects bool/non-numeric/NaN/infinity
* odds_format supports "american" and "decimal" only
* odds validation is format-specific: american rejects 0; decimal rejects <= 1.0
* odds_found_at is required non-empty stripped string (future CLV depends on observed price timestamp)
* source is optional stripped string or None
* metadata defaults to {}, must be dict, shallow copied

Pure/in-memory only. Imports math only. No RSB module imports.

Tests:

* tests/test_odds_snapshot.py
* Full local test result after merge: 1125 passed
* GitHub Actions/checks for PR #28: 8 successful, 0 failing, 0 pending

v0.2.6 intentionally did not add:

* live odds API
* scraping
* provider credentials
* sportsbook integration
* implied probability
* edge
* EV
* prop candidate attachment
* settlement
* CLV calculation
* ranking
* picks
* recommendations
* locks
* parlays
* runtime wiring
* database changes
* new dependencies
* CI changes

Important scope clarification:
The v0.2.6 omissions are version-scoped only. RSB's long-term product goal still includes
odds providers, sportsbook APIs, player prop data, simulations, injury/lineup data, historical
results, model calibration, and eventually ranked +EV prop/pick candidates. v0.2.6 avoided
those items only because it was limited to pure odds snapshot / provider record normalization primitives.

═══════════════════════════════════════════════════════
v0.2.5 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.5 added pure prop/pick candidate schema primitives.

New files:

* src/prop_candidate.py
* tests/test_prop_candidate.py

Implemented in src/prop_candidate.py:

* PropCandidateValidationError
* normalize_sport(sport)
* normalize_league(league)
* normalize_market_type(market_type)
* normalize_selection(selection)
* build_prop_candidate(...) -> dict

Return shape (always exactly 14 keys):

{
    "sport":         str,
    "league":        str,
    "event_id":      str,
    "market_type":   str,
    "selection":     str,
    "line":          float | None,
    "player_id":     str | None,
    "player_name":   str | None,
    "team_id":       str | None,
    "team_name":     str | None,
    "opponent_id":   str | None,
    "opponent_name": str | None,
    "created_at":    str | None,
    "metadata":      dict,
}

No RSB runtime module imports.
Pure/in-memory only. No DB, no wiring.

Tests:

* tests/test_prop_candidate.py
* Full local test result after merge: 956 passed
* GitHub Actions/checks for PR #26: 8 successful, 0 failing, 0 pending

v0.2.5 intentionally did not add:

* odds
* sportsbook
* provider
* implied probability
* edge
* EV
* settlement
* CLV
* ranking
* picks
* recommendations
* locks
* parlays
* runtime wiring
* database changes
* new dependencies
* CI changes

Important scope clarification:
The v0.2.5 omissions are version-scoped only. RSB's long-term product goal still includes
odds providers, sportsbook APIs, player prop data, simulations, injury/lineup data, historical
results, model calibration, and eventually ranked +EV prop/pick candidates. v0.2.5 avoided
those items only because it was limited to pure prop/pick candidate identity schema primitives.

═══════════════════════════════════════════════════════
v0.2.4 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.4 added odds conversion helpers and pure EV math primitives.

Files modified:

* CHANGELOG.md
* CLAUDE.md
* README.md
* config/model_config.json
* pyproject.toml
* src/ev.py
* src/odds.py
* tests/test_ev.py
* tests/test_odds.py
* tests/test_paths.py

Implemented in src/odds.py:

* american_to_decimal_odds(odds) -> float
* decimal_to_american_odds(odds) -> float

Implemented in src/ev.py:

* EVValidationError(ValueError)
* calculate_expected_value(model_probability, decimal_odds) -> float
* Validated backward-compatible wrappers:
  - american_to_decimal(odds)
  - implied_probability(odds)
  - ev_per_unit(model_probability, odds)
  - edge(model_probability, odds)

Formula:

calculate_expected_value(model_probability, decimal_odds):
    return model_probability * decimal_odds - 1.0

Validation summary:

* odds conversion helpers reject bool, non-numeric values, NaN, and infinity
* american_to_decimal_odds rejects zero American odds
* decimal_to_american_odds rejects decimal odds <= 1.0
* calculate_expected_value validates model_probability through odds.validate_probability()
* invalid model_probability raises OddsValidationError
* calculate_expected_value validates decimal_odds locally
* invalid decimal_odds raises EVValidationError
* decimal_odds must be numeric, non-bool, not NaN, not infinite, and > 1.0
* no rounding

Compatibility detail:

* Runtime modules still import legacy ev.py public names.
* v0.2.4 intentionally preserved those names as validated wrappers.
* No runtime callers were edited.

Tests:

* tests/test_ev.py
* tests/test_odds.py
* Full local/GitHub test result after merge: 830 passed
* GitHub Actions: 8 successful, 0 failing, 0 pending

v0.2.4 intentionally did not add:

* live odds API integration
* odds-api / github.com/odds-api/odds-api integration
* scraping
* sportsbook API calls
* provider integration changes
* database changes
* runtime wiring
* picks
* recommendations
* locks
* parlays
* Kelly/staking
* bankroll advice
* UI/API/frontend
* new dependencies
* CI changes
* data file changes

Important scope clarification:
The v0.2.4 omissions are version-scoped only. RSB's long-term product goal still includes
odds providers, sportsbook APIs, player prop data, simulations, injury/lineup data, historical
results, model calibration, and eventually ranked +EV prop/pick candidates. v0.2.4 avoided
those items only because it was limited to pure odds conversion and EV math primitives.

═══════════════════════════════════════════════════════
v0.2.3 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.3 added pure backtest review overlay primitives.

New files:

* src/backtest_review.py
* tests/test_backtest_review.py

Files modified:

* CHANGELOG.md
* CLAUDE.md
* README.md
* config/model_config.json
* pyproject.toml
* tests/test_paths.py

Implemented in src/backtest_review.py:

* BacktestReviewValidationError
* MIN_EVALUATED_ROWS = 10
* ACCURACY_STRONG_THRESHOLD = 0.60
* ACCURACY_WEAK_THRESHOLD = 0.40
* VALID_REVIEW_STATUSES = frozenset({"strong", "mixed", "weak", "insufficient_data"})
* build_backtest_review(report: dict) -> dict

Return shape (always exactly 5 keys):

{
    "review_status": str,
    "needs_review": bool,
    "skipped_row_flag": bool,
    "evaluated_rows": int,
    "accuracy": float | None,
}

Validation summary:

* report must be a dict
* required keys: evaluated_rows, skipped_rows, overall
* overall must be a dict containing accuracy
* evaluated_rows must be a non-bool int >= 0
* skipped_rows must be a non-bool int >= 0
* accuracy may be None
* non-None accuracy must be a non-bool numeric value in [0.0, 1.0]
* NaN accuracy is rejected
* valid numeric accuracy is returned as float

Classification logic:

* evaluated_rows < 10 or accuracy is None → insufficient_data
* accuracy >= 0.60 → strong
* accuracy >= 0.40 → mixed
* otherwise → weak

Review flags:

* needs_review = review_status in ("weak", "insufficient_data") or skipped_rows > 0
* skipped_row_flag = skipped_rows > 0

No imports. NaN accuracy is rejected using an import-free self-inequality check.
No RSB module imports.
Not wired into any existing module in this version.

Tests:

* tests/test_backtest_review.py
* Full local/GitHub test result after merge: 744 passed

v0.2.3 intentionally did not add:

* betting recommendations
* picks
* parlays
* EV calculation
* Kelly/staking
* runtime wiring
* database/schema changes
* data file changes
* dependencies
* CI changes
* self-learning automation

═══════════════════════════════════════════════════════
v0.2.2 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.2 added pure candidate evaluation record and pass reason primitives.
This is the first version that defines a structured candidate vocabulary and record format.

New files:

* src/candidate_evaluation.py
* tests/test_candidate_evaluation.py

Files modified:

* CHANGELOG.md
* CLAUDE.md
* README.md
* config/model_config.json
* pyproject.toml
* tests/test_paths.py

Implemented in src/candidate_evaluation.py:

* CandidateEvaluationValidationError
* VALID_CANDIDATE_STATUSES
* VALID_PASS_REASONS
* normalize_candidate_status(status: str) -> str
* normalize_pass_reason(reason: str) -> str
* validate_pass_reasons(pass_reasons) -> list[str]
* build_candidate_evaluation(status, edge=None, pass_reasons=None) -> dict
* _validate_edge() — private helper, not exported

Only import: stdlib math (for NaN/inf checks in _validate_edge).
No imports from odds.py, edge.py, review_taxonomy.py, review_notes.py, database, or runtime modules.
Not wired into any existing module in this version.

Constants:

VALID_CANDIDATE_STATUSES = {
    "candidate",
    "rejected",
    "not_evaluable",
}

VALID_PASS_REASONS = {
    "edge_below_minimum",
    "missing_model_probability",
    "missing_implied_probability",
    "invalid_model_probability",
    "invalid_implied_probability",
    "market_semantics_unclear",
    "data_quality_concern",
    "manual_review_required",
    "unknown",
}

Return shape (always exactly 3 keys):

{
    "status": "candidate",
    "edge": 0.05,
    "pass_reasons": [],
}

or:

{
    "status": "rejected",
    "edge": 0.01,
    "pass_reasons": ["edge_below_minimum"],
}

or:

{
    "status": "not_evaluable",
    "edge": None,
    "pass_reasons": ["missing_model_probability"],
}

Structural invariants enforced by build_candidate_evaluation():

* status="candidate"     → pass_reasons must be exactly []
* status="rejected"      → pass_reasons must have at least one validated reason
* status="not_evaluable" → pass_reasons must have at least one validated reason
* Violating these raises CandidateEvaluationValidationError

Operation order inside build_candidate_evaluation():

1. Normalize and validate status → CandidateEvaluationValidationError if unknown
2. Validate edge via _validate_edge() → CandidateEvaluationValidationError if invalid
3. Normalize and validate pass_reasons via validate_pass_reasons() → list
4. Enforce structural invariant → CandidateEvaluationValidationError if violated
5. Return dict

Edge validation rules (_validate_edge):

* None → None (allowed on any status)
* bool → reject
* non-numeric → reject
* NaN → reject
* infinity → reject
* edge < -1.0 → reject
* edge > 1.0 → reject
* valid → return float(edge)

Normalization contract (matching prior RSB primitive pattern):
strip whitespace → lowercase → replace spaces and hyphens with underscores → validate

validate_pass_reasons(None) returns [].
pass_reasons accepts list or tuple; tuple is returned as list.
Order is preserved. Duplicates are preserved.
Empty or whitespace-only pass reasons raise CandidateEvaluationValidationError.
Unknown pass reasons raise CandidateEvaluationValidationError.

Tests:

* tests/test_candidate_evaluation.py
* Full local/GitHub test result after merge: 643 passed

v0.2.2 intentionally did not add:

* minimum_edge parameter
* threshold comparison
* auto-generated pass reasons
* data_quality parameter
* recommendations
* picks
* parlays
* EV or Kelly math
* runtime wiring
* database schema changes
* model or simulator changes
* CI changes
* new dependencies
* any imports from odds.py, edge.py, review_taxonomy.py, or review_notes.py

═══════════════════════════════════════════════════════
v0.2.1 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.1 added pure edge calculation primitives.
This is the first version that computes a numeric edge signal.

New files:

* src/edge.py
* tests/test_edge.py

Implemented in src/edge.py:
* calculate_edge()

Imports only validate_probability from src/odds.py.
No stdlib imports. No other RSB module imports.

Function contract:

calculate_edge(model_probability, implied_probability):
* Validates model_probability via validate_probability() from odds.py.
  Raises OddsValidationError on invalid input.
* Validates implied_probability via validate_probability() from odds.py.
  Raises OddsValidationError on invalid input.
* Returns model_probability - implied_probability as a plain Python float.
* No rounding. Native Python float arithmetic.
* Positive: model probability is higher than sportsbook implied probability.
* Negative: model probability is lower than sportsbook implied probability.
* Zero: they are equal.
* Valid edge range: -1.0 to 1.0 (natural consequence of valid probability inputs).

No EdgeValidationError. All validation is probability validation delegated to odds.py.

Tests:
* tests/test_edge.py — 33 tests
* Full project test count: 545 passing

═══════════════════════════════════════════════════════
v0.2.0 SUMMARY — FOR REFERENCE
═══════════════════════════════════════════════════════

v0.2.0 added pure odds and implied probability conversion primitives.
This is the first +EV math foundation version.

New files:

* src/odds.py
* tests/test_odds.py

Implemented in src/odds.py:
* OddsValidationError
* american_to_implied_probability()
* decimal_to_implied_probability()
* fractional_to_implied_probability()
* validate_probability()

Imports only stdlib math. No RSB module imports.

Tests:
* tests/test_odds.py — 80 tests
* Full project test count: 512 passing

═══════════════════════════════════════════════════════
STRICT CLAUDE RULES — ALWAYS ACTIVE
═══════════════════════════════════════════════════════

* Do not inspect files until ChatGPT provides a version-specific prompt.
* Do not plan until ChatGPT provides a version-specific prompt.
* Do not edit any files before ChatGPT approves a plan.
* Do not commit.
* Do not push.
* Do not create PRs.
* Do not use auto mode.
* Do not use "allow all edits during this session."
* Do not use update-config.
* Do not create memory/ files or unrelated files.
* Do not modify real data files:
  * data/input/*.json
  * data/output/*.json
  * data/worldcup_ai.db
* Do not add pip dependencies without explicit ChatGPT approval.
* Do not change CI without explicit ChatGPT approval.
* Do not loosen betting, data, or model guardrails.
* Do not change simulator coefficients unless explicitly approved.
* Do not add betting recommendations unless explicitly approved.
* Do not add parlay logic unless explicitly approved.
* Do not wire stage_market.py, review_taxonomy.py, review_notes.py, odds.py,
  edge.py, candidate_evaluation.py, backtest_review.py, ev.py, prop_candidate.py,
  odds_snapshot.py, prop_result.py, candidate_ev.py, candidate_ranking.py,
  candidate_report.py, market_capability.py, or mlb_capability.py into existing
  modules unless explicitly approved.
* Show each proposed file edit and wait for approval before writing it.
* Do not update Handoffs/ files unless explicitly instructed by the user with ChatGPT approval.

═══════════════════════════════════════════════════════
HANDOFF MAINTENANCE RULE
═══════════════════════════════════════════════════════

After every completed version, or whenever roadmap/guardrails/workflow/current-state decisions
change, update both:
- Handoffs/ChatGpt_Handoff.txt
- Handoffs/Claude_handoff.txt

Claude may update these files only when explicitly instructed. Claude must not invent roadmap
changes or silently rewrite handoffs. ChatGPT remains the gatekeeper for what belongs in the
handoffs.

═══════════════════════════════════════════════════════
WORKFLOW — FOR REFERENCE ONLY
═══════════════════════════════════════════════════════

1. ChatGPT finalizes version scope.
2. ChatGPT writes a version-specific implementation prompt for Claude.
3. Claude receives that prompt, runs approved read-only verification, inspects approved files,
   and returns a plan only.
4. User pastes Claude's plan into ChatGPT for approval.
5. ChatGPT approves, refines, or rejects the plan.
6. Claude implements only approved scope, one file at a time with per-edit approval.
7. Ryan runs git/status/diff/test review commands directly in Terminal.
   Claude provides the review package commands to run.
   For long review output, ChatGPT should give Ryan a command that writes the full review
   package to /tmp/rsb_<version>_review.txt and opens it, instead of making Ryan scroll
   through Terminal.
   Use git diff HEAD in review packages when staged changes may exist.
8. User pastes review package output into ChatGPT.
9. ChatGPT approves commit.
10. User commits manually.
11. Claude never commits, even after ChatGPT approval.

═══════════════════════════════════════════════════════
CLAUDE CLI BRANCH RULE
═══════════════════════════════════════════════════════

Ryan uses Claude CLI from Terminal by running:

claude

Claude CLI inherits the current Terminal working directory and active git branch.
Because of this, implementation workflow must be:

1. ChatGPT approves the version/chore plan.
2. Ryan switches to main.
3. Ryan pulls latest main.
4. Ryan verifies main is clean.
5. Ryan creates/switches to the approved feature/chore branch himself using:

   git switch -c <branch-name>

6. Ryan verifies the active branch with:

   git branch --show-current

7. Only then does Ryan run claude from that same Terminal session/repo.
8. Claude must inspect and edit only from the branch it was launched in.

Do not tell Ryan to open Claude for implementation while still on main.

Do not tell Ryan to create the branch after Claude has already started implementation.

For plan-only inspection, Claude may inspect files before implementation only if Ryan
intentionally launches Claude for planning, but any implementation session must start
after Ryan is already on the approved branch.

Additional workflow rules:

* Ryan runs git/status/diff/test validation commands himself in Terminal and pastes
  outputs into ChatGPT.
* Claude should not run git validation as the source of truth unless Ryan explicitly
  asks for a quick local observation.
* ChatGPT remains the gatekeeper for approval before commits, pushes, PRs, and merges.

═══════════════════════════════════════════════════════
WHAT I NEED FROM YOU NOW
═══════════════════════════════════════════════════════

Do not inspect files yet.
Do not plan v0.3.0 or any specific next version yet.
Do not edit anything.
Do not run commands yet.
Do not commit.

Acknowledge this updated RSB context. v0.2.11 and v0.2.12 are complete and merged.
v0.3.0 is part of the approved remaining roadmap batch but is not yet approved for
implementation — do not invent or lock in a v0.3.0 (or v0.2.13) scope. Wait for
ChatGPT to provide the next version-specific Claude prompt. Implementation still
proceeds one version per branch and one PR at a time; do not create a new version
branch until that prompt is provided.

═══════════════════════════════════════════════════════
2026-08-11 ROADMAP REASSESSMENT
═══════════════════════════════════════════════════════

This section records a roadmap decision made on 2026-08-11, on branch
chore/update-roadmap-before-v0.3.1, before any v0.3.1 code was written. It does not
retroactively rewrite any earlier historical section above — those remain accurate to
what was known/decided at the time they were written. (The immediately preceding
section, for example, reflects the v0.2.11/v0.2.12 moment in time, not current state.)

Why the old sequence was superseded:
By v0.3.0, RSB had accumulated substantial reusable downstream decision infrastructure
(candidate identity, odds/EV math, the Candidate Evaluation Contract, ranking, reporting,
settlement primitives, backtest metrics, sport/market capability primitives including an
MLB capability seed) while the actual MLB probability-generation engine still did not
exist. The previous tentative sequence (v0.3.1 Legacy Candidate Translation, v0.3.2
Canonical Odds Snapshot Reconciliation, v0.3.3 Candidate Persistence Framework, v0.3.4
First MLB Runtime Skeleton) over-prioritized infrastructure around the frozen legacy
World Cup system while that missing modeling middle remained unaddressed. Reviewing an
external MLB modeling reference highlighted that RSB's reusable decision infrastructure
had advanced further than its sport-specific probability-generation layer. That
comparison is the origin of this reassessment, not a template RSB is copying — the
architectural conclusions stand on RSB's own needs.

Roadmap decision:
* v0.3.1 — MLB Statcast Data Foundation is now APPROVED NEXT at the roadmap/objective
  level. Implementation has not started. Its detailed implementation architecture is not
  yet approved — a separate v0.3.1 inspection/planning stage requires ChatGPT review and
  approval before coding begins.
* v0.3.2 (MLB Plate-Appearance Dataset & Rate Foundation), v0.3.3 (MLB Plate-Appearance
  Probability Baseline), and v0.3.4 (MLB Walk-Forward Evaluation & Calibration) remain a
  directional planning batch — not independently approved implementation scopes. Version
  numbers/titles may be reassessed after preceding versions teach us more.
* Legacy Candidate Translation is removed from the MLB critical path, not necessarily
  deleted forever — it may return if a concrete future consumer requires it.
* Canonical odds-snapshot reconciliation and candidate persistence remain real future
  needs; their eventual design is deferred until the real MLB pipeline gives them
  concrete requirements, rather than being designed around guesses now.

Final sport scope:
* MLB is the first new sport engine — the active next modeling domain.
* NBA is the second and final new sport engine, after MLB.
* No NFL, NHL, or further sport expansion is currently planned.
* World Cup remains the existing, frozen legacy/reference implementation — unchanged by
  this decision.

Future non-sports project:
* After MLB and NBA reach a defined finished RSB state, RSB does not default to adding
  further sports. A separate, non-sports probabilistic forecasting/quantitative project
  (domain undecided) may follow later, built on lessons RSB proves out — not a fork or
  rebrand of RSB. No finance-specific abstractions (tickers, assets, trading signals,
  portfolios) belong in RSB's current architecture.

New durable document:
* docs/MLB_NBA_ROADMAP.md was added as the durable roadmap/project-direction document
  for this decision, the v0.3.1 approval, and the v0.3.2-v0.3.4 directional batch. It
  does not name the external reference project or any identifying detail about it.

Immediate next step after this docs chore merges:
* Perform the v0.3.1 inspection/planning stage for MLB Statcast Data Foundation. No MLB
  code is written until ChatGPT approves that implementation plan.

Handoff hygiene note (not part of this chore):
* Both handoff files have grown past 2,000 lines with substantial duplication between
  them. That is a real future documentation-hygiene item but is explicitly out of scope
  for this roadmap-synchronization chore and was not touched here.

═══════════════════════════════════════════════════════
2026-08-11 v0.3.1 STAGE 2 IMPLEMENTATION COMPLETE
═══════════════════════════════════════════════════════

This section records the Stage 2 implementation of v0.3.1 — MLB Statcast Data
Foundation, on branch feature/v0.3.1-mlb-statcast-data-foundation, after the Stage 1
inspection/planning architecture was reviewed and approved by ChatGPT. Implementation
was performed by Claude under explicit authorization; no scope beyond the approved
Stage 1 architecture (with its stated corrections) was added.

What was built:
* src/mlb/statcast_import.py — load_statcast_export(): reads a local, already-downloaded
  Baseball Savant CSV export. No network access of any kind. Validates file
  existence/readability/UTF-8 decoding/CSV header structure. The adapter recognizes 33
  mapped provider columns; mandatory normalized fields require their backing source
  headers (17 required columns), while the 16 provider-nullable mapped columns may be
  entirely absent from the header and normalize to null. Unmapped columns are tolerated
  and preserved only in the raw layer. Preserves raw bytes and source row order exactly.
  Records operator-declared declared_query/requested_date_range honestly; does not infer
  source_downloaded_at from filesystem metadata (it is None unless the operator
  explicitly supplies it).
* src/mlb/statcast_normalize.py — normalize_statcast_row()/normalize_statcast_snapshot():
  builds the RSB-owned normalized pitch-level contract from the approved source field
  mapping. Fail-closed for required identity/chronology/game-context/pre-pitch fields;
  legitimately provider-nullable fields normalize to None. Baseball sanity bounds:
  balls 0-3, strikes 0-2, outs 0-2, inning >= 1. rsb_pitch_id is a deterministic SHA-256
  of (source_game_id, at_bat_number, pitch_number), validated unique per snapshot.
  pa_event_raw is an unvalidated passthrough of the provider's events column — no
  pa_terminal derivation (v0.3.2 scope, not touched). No venue field.
* src/mlb/statcast_snapshot.py — create_statcast_snapshot(): orchestrates
  import -> normalize -> persist. Writes raw .csv.gz (gzip of exact original bytes),
  normalized .jsonl.gz (gzip of canonical deterministic JSONL), and a .manifest.json with
  full provenance, ingestion timing, schema version, row counts, and SHA-256 checksums
  computed over uncompressed bytes before gzip. snapshot_id is
  <ingestion_timestamp>_<raw_sha256_prefix12> — content-and-ingestion-event-aware, per
  the Stage 1 correction: re-ingesting identical raw bytes later produces the same raw
  hash but a different snapshot_id and therefore a different normalized_content_sha256.
  Existing snapshot artifacts are never overwritten (StatcastSnapshotError); overlapping
  snapshots coexist.
* src/paths.py — added DATA_MLB_DIR, get_mlb_data_dir() (RSB_MLB_DATA_DIR override,
  matching the existing RSB_*_PATH convention), and get_mlb_manual_input_dir() /
  get_mlb_raw_statcast_dir() / get_mlb_normalized_statcast_pitch_dir() /
  get_mlb_snapshot_dir() for the approved data/mlb/{manual_input,raw/statcast,
  normalized/statcast_pitch,snapshots} layout. data/mlb/ added to .gitignore.

Tests: 218 new tests across tests/test_statcast_import.py, tests/test_statcast_normalize.py,
tests/test_statcast_snapshot.py, and MLB path-override additions to tests/test_paths.py,
plus a tiny synthetic fixture at tests/fixtures/mlb/statcast_sample.csv. Full suite: 2156
passed (1938 baseline + 218 new), 0 failures, 0 skips, zero network access required.

Dependencies: zero added. CSV parsing uses the Python standard library csv module, not
pandas — pandas was already a project dependency but was judged unnecessary for this
narrow, fully-controlled ingestion layer.

Architecture-boundary confirmation: no MLB/Baseball Savant network access anywhere in
src/mlb/; no World Cup runtime or database.py changes; data/worldcup_ai.db untouched; no
plate-appearance aggregation, terminal-PA taxonomy, feature engineering, probability
generation, modeling, calibration, simulation, sportsbook odds, or candidate EV/ranking
work; AST-based tests confirm the new MLB modules do not import run_slate, database,
data_quality, or market_selector.

Deviations from the approved Stage 1 plan: none identified during implementation.

Status: IMPLEMENTATION COMPLETE — READY FOR CHATGPT REVIEW. Not committed, pushed, or
merged by Claude — Ryan retains git validation/commit/push/PR per the standing workflow.
docs/progress/progress-data.js activeWorkspace and currentGate were updated to reflect
this state; repositoryBaseline/readiness/modules remain the merged-main snapshot pending
actual merge (per the workflow's post-merge verification step).

═══════════════════════════════════════════════════════
2026-08-11 v0.3.1 FOCUSED PRE-COMMIT CORRECTION PASS
═══════════════════════════════════════════════════════

ChatGPT's independent implementation review of v0.3.1 found the overall architecture
sound (correct branch, clean diff, zero dependency changes, no network access, no World
Cup integration, 2117 passed) but flagged six contained pre-commit correctness/provenance
issues. Claude applied focused corrections only, on the same feature branch, without
broadening scope, adding dependencies, or touching PA/model/simulation work:

1. declared_query restored to structured, JSON-serializable dict (string keys; rejects
   non-serializable values rather than silently converting) instead of free-form prose,
   matching the approved Stage 1 manifest contract. Empty dict is explicitly valid
   ("no additional filters beyond requested_date_range").
2. source_provider is now fixed to "baseball_savant" (SOURCE_PROVIDER constant) — no
   longer an overridable argument on create_statcast_snapshot() — so the manifest and
   every normalized row always agree on source_provider by construction.
3. normalize_statcast_snapshot() now validates snapshot_id/source_provider/
   requested_date_range before iterating any rows, so a zero-row snapshot still enforces
   these invariants (previously partially delegated to per-row validation, which a
   zero-row snapshot would never reach).
4. Ingestion timestamps (ingestion_started_at/ingestion_completed_at, and therefore the
   snapshot_id timestamp component) now carry microsecond precision, preventing
   same-second collisions.
5. source_downloaded_at, when supplied, now requires an explicit UTC offset or "Z"
   (naive datetimes rejected) and is normalized to a canonical UTC "...Z" string.
6. Corrected a documentation error: the source field mapping is 33 provider columns, not
   32 as originally stated (the field contract itself was never wrong, only the doc count).

Tests: 29 new/changed tests added across the three v0.3.1 test files covering all six
corrections. Full suite after the correction pass: 2146 passed (1938 baseline + 208 new
v0.3.1 tests total), 0 failures, 0 skips.

Deviations from the correction-pass instructions: none.

Status: CORRECTION PASS COMPLETE — READY FOR CHATGPT REVIEW. Not committed, pushed, or
merged by Claude.

═══════════════════════════════════════════════════════
2026-08-11 v0.3.1 FINAL OPTIONAL-HEADER CORRECTION
═══════════════════════════════════════════════════════

ChatGPT's final implementation review found the pre-commit correction pass otherwise
approved (correct branch, clean diff, zero dependency changes, no network access, no
World Cup scope leakage, no stale 32-column documentation, 2146 passed) but flagged one
remaining contract violation: statcast_import.py's CSV header check required all 33
mapped provider columns to be present, incorrectly treating fields statcast_normalize.py
classifies as provider-nullable/optional as mandatory source headers. A CSV genuinely
missing an optional column such as launch_angle, hit_distance, pitch_name, or sv_id was
being rejected at import time instead of normalizing that field to null.

Correction: split EXPECTED_SOURCE_COLUMNS (all 33 mapped columns the adapter recognizes)
from a new REQUIRED_SOURCE_COLUMNS (the 17 columns backing
statcast_normalize.REQUIRED_NORMALIZED_FIELDS). REQUIRED_SOURCE_COLUMNS is derived
directly from statcast_normalize.SOURCE_FIELD_MAPPING and REQUIRED_NORMALIZED_FIELDS
(statcast_import.py now imports these) rather than duplicating a second, independently
maintained field classification. Only REQUIRED_SOURCE_COLUMNS must be present in the CSV
header; the other 16 provider-nullable mapped columns may be entirely absent, in which
case the corresponding row value is treated as missing (same as csv.DictReader's
row.get() behavior for an absent key) and normalizes to None downstream — no change was
needed in the row-normalization logic itself, since it already used dict.get() and
already treated None the same as a blank string for nullable fields. The raw layer is
unaffected: exact supplied file bytes are still preserved unchanged regardless of which
optional columns are present.

Tests: corrected the test that expected a missing launch_angle header to fail (it now
expects success), and added 10 new tests proving: a genuinely required column missing
still fails with the column named in the error; several optional columns missing
individually still succeed; the normalized record contains the corresponding key with
value None when its source header is entirely absent (proven against a row that DOES
have a non-blank value for that field when the column is present, to show the None comes
from column absence, not blank value); multiple simultaneous missing optional headers
all normalize to None while unaffected required fields remain correct; and a CSV
containing only the 17 required headers (all 16 optional headers absent) still imports
and normalizes successfully. Full suite: 2156 passed (1938 baseline + 218 new v0.3.1
tests total), 0 failures, 0 skips.

Deviations: none.

Status: FINAL CORRECTION COMPLETE — READY FOR CHATGPT REVIEW. Not committed, pushed, or
merged by Claude.
